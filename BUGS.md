# UA News — Журнал багів (BUGS.md)

> Формат: №, дата, хто помітив, симптом, причина, рішення, статус.
> Нові баги — зверху.

---

## 🔴 Відкриті баги — тепер у GitHub Issues

Відкриті баги й задачі більше НЕ ведемо в цьому файлі — вони живуть у
[GitHub Issues](https://github.com/blazhkevych/telegram-news-bot/issues),
щоб мати стабільне посилання, видиме з телефона, і зв'язок із комітами
(`fix: ... (closes #N)`). Цей файл лишається **архівом виправленого** —
детальні розбори «симптом → причина → рішення → верифікація» по закритих
багах (те, для чого Issues незручні: їх закривають і забувають).

Поточні відкриті:
- **#1 — БАГ-006:** якість переказу AI (вигадана стать, повтори, «вода»).
- **#3:** погода «дані недоступні» для частини міст у ранковому дайджесті.
- **#6 — залишок БАГ-012:** хибні злиття різних новин у курації — лексичний
  захист поставлено 03.08, лишається семантичний.
- **#4:** дублі однієї події в каналі. Фікс 18.07 (URL-и групи + posted_title)
  бойову перевірку 19.07 НЕ пройшов (Wildberries ×6, поїзд ×4 — розбір у
  STATUS.md). 19.07 додано другий рівень: дедуп по згенерованому заголовку
  (лексичний + семантичний LLM) ДО публікації — чекає бойової перевірки.

- **БАГ-014 (issue ще не заведено):** «трендовість» у сортуванні кандидатів
  вимірює не популярність теми, а те, скільки прогонів новина провисіла в
  RSS-стрічці. Розбір нижче, у розділі «🔎 Знайдені, ще не виправлені».

Коли закриваєш баг — переноси сюди повний розбір у розділ «✅ Виправлені»
й закривай відповідний issue комітом.

---

## 🔎 Знайдені, ще не виправлені

### БАГ-014 — «трендовість» вимірює застарілість новини, а не популярність теми
- **Дата:** знайдено 2026-08-08 (ревізія за добу, сесія 18).
  **Помітив:** Claude (аналіз таблиці `seen_topics` у бойовій БД).
  **Issue:** ще не заведено. **Статус:** відкритий, у код не лізли.
- **Симптом:** топ ключових слів за добу в `seen_topics` — це англомовний
  мотлох із техно- і лайфстайл-стрічок, який у канал майже не потрапляє:
  `trough` 78, `denying` 71, `‘rubberz’` 71, `wacom’s` 65, `movinkpad` 65,
  `mexico;` 64. При цьому медіана `count` серед 1065 нових за добу ключів — 2,
  а 90-й перцентиль — 12. Тобто розподіл двогорбий: майже все має 1–2, а
  окрема жменька (17 ключів із count ≥ 40) вилітає під стелю.
- **Причина (перевірено по коду):** у `fetch_news` (bot.py, рядки 678–679)
  `update_topic_count()` викликається для КОЖНОГО кандидата на КОЖНОМУ
  прогоні — незалежно від того, опублікували новину, відсіяли її чи вона
  просто далі лежить у стрічці. Прогонів на добу максимум 96 (`cron */15`).
  Числа 78, 71, 65 — це саме частка від 96, тобто `count` ≈ «скільки годин
  запис провисів у entries[:5]», а не «скільки джерел написали про подію».
  Найбільше накручують собі лічильник ті записи, які курація щоразу
  відхиляє: вони не зникають зі стрічки й рахуються знову і знову.
- **Наслідок:** `get_topic_count` — ДРУГИЙ ключ сортування кандидатів
  (bot.py:697, після `ukraine_score`). Отже, при рівному `ukraine_score`
  нагору піднімається найлежаліша новина, а не найгарячіша. Це прямо
  суперечить обіцянці каналу «новини в реальному часі». Масштаб шкоди
  обмежений тим, що `ukraine_score` домінує в українському кошику; але в
  англомовному (`en[:3]`) `ukraine_score` майже завжди 0, і там цей
  зіпсований лічильник вирішує все. Це та сама межа, об яку в сесії 16
  спіткнувся аудит джерел: нішеві en-стрічки або голодують, або проходять
  саме найстарішим матеріалом.
- **Побічне:** `seen_topics` росте без прибирання — 71 460 рядків,
  +1065 за добу, і `get_topic_count` робить SELECT на кожне слово кожного
  кандидата кожного прогону. На поточних обсягах не болить, але це
  накопичувальний борг.
- **Ще не полагоджено. Напрямок фіксу (обговорити до реалізації):** рахувати
  подію ОДИН раз — наприклад, інкрементувати лічильник не по кожному
  кандидату кожного прогону, а лише коли URL бачимо вперше (`skipped`/
  `published` уже дають хеш URL), або взагалі замінити `seen_topics` на
  «скільки РІЗНИХ джерел у злитій групі» — це вимірює саме те, що хотілось,
  і вже рахується в `merge_by_event`. Другий варіант простіший і прибирає
  цілу таблицю; вирішувати разом з issue #7 (`published.source`).
- **Тимчасове пом'якшення не потрібне:** канал працює, ефект — погіршення
  свіжості в частині слотів, не збій.

---

## ✅ Виправлені / Закриті

### БАГ-013 — заголовок поста = сам лише емодзі («🚨»): дірка в дедупі
- **Дата:** знайдено 2026-08-06 (аудит сесії 16), виправлено 2026-08-07.
  **Помітив:** Claude (аналіз `posted_title` у бойовій БД). **Issue:** #5.
- **Симптом:** у `published.posted_title` замість заголовка стоїть саме лише
  емодзі. За весь час — **5 випадків**: msg 5279, 5516 (22.07), 7598 (30.07),
  9065, 9613 (06.08). Тобто ~раз на 4 дні, а не «3 за 3 тижні», як оцінювалось
  спочатку: аудит по 8 добах у сесії 17 знайшов два свіжі випадки.
- **Причина:** у `main()` заголовок брався як перший **непорожній** рядок
  відповіді моделі: `next((ln.strip()[:200] for ln in post_text.splitlines()
  if ln.strip()), None)`. Моделі резервного ланцюга регулярно ставлять емодзі
  окремим рядком («🚨\nЗеленський підписав…»), і в `headline` потрапляло «🚨».
- **Наслідки — два, і НЕ ті, що припускались спочатку.** Первісна гіпотеза
  («наступна новина з таким самим заголовком буде вбита як `duplicate_event`»)
  перевірена по коду й виявилась вірною лише наполовину:
  1. **Лексичний шар вбити НЕ міг.** `_title_words("🚨")` дає порожню множину
     (`re.findall(r"[а-яіїєґёa-z0-9']+", …)`), тому `is_duplicate_title`
     повертає `False` для такого кандидата і пропускає такий рядок при
     порівнянні (`if not old: continue`). Хибного спрацювання тут не буває.
  2. **Семантичний шар вбити МІГ.** `is_semantic_duplicate` бере
     `COALESCE(posted_title, title)` і показує «🚨» моделі в списку
     опублікованих; кандидат «🚨» проти збереженого «🚨» цілком отримав би
     вердикт ДУБЛЬ, а тоді `mark_skipped(..., "duplicate_event")` кладе ВСЮ
     злиту групу. Не спрацювало лише тому, що з 5 випадків жодні два не
     потрапили в одне 24-годинне вікно (5 рядків, 2 різні емодзі).
  3. **А от що ставалось ЩОРАЗУ (і чого гіпотеза не називала):** пост із
     заголовком «🚨» на всі 24 години лишається в базі дедупу **без
     заголовка**. Справжні дублі саме цієї події не ловить ні лексичний шар,
     ні семантичний — обидва бачать емодзі замість тексту. Тобто це не
     потенційна, а вже реалізована дірка в покритті — 5 разів.
- **Рішення:** заголовком стає перший рядок, у якому є **хоча б 3 літери**
  (`len(re.findall(r"[^\W\d_]", ln)) >= 3`). Якщо змістовного рядка немає
  взагалі — `None`, а не відкат на перший непорожній: `posted_title = NULL`
  безпечний, бо і курація, і `is_semantic_duplicate` мають фолбек
  `COALESCE(posted_title, title)` на RSS-заголовок, а «🚨» у базі — ні.
  Сам пост у канал виходить як є; його якість — зона `is_bad_output`.
- **Верифіковано:** тест тягне РЕАЛЬНИЙ вираз із `bot.py` регуляркою (щоб не
  перевіряти власну копію логіки). Обидва реальні кейси розбираються
  правильно; «⚡️» без тексту → `None`; рядок із 2 літер пропускається;
  регрес не зачепив звичайні пости, емодзі В ОДНОМУ рядку із заголовком,
  порожні рядки на початку, англомовні заголовки, обрізання до 200 символів.
  На бойовій БД: 5 отруєних `posted_title`, з них 3 збіги між собою — тобто
  ризик був справжній, просто рознесений у часі.
- **Статус:** ✅ 2026-08-07 (closes #5).

---

### БАГ-012 — курація злипає РІЗНІ новини в одну «подію»: вигадані факти в постах + тихо вбиті новини
- **Дата:** 2026-08-03. **Помітив:** власник (показав пост msg 5200) → Claude
  (аналіз бойової published.db і коду курації).
- **Симптом (два різні на вигляд, корінь один):**
  1. 21.07, msg 5200, пост «⚡️ Федоров залишився лише на одній посаді» містив
     цілий ВИГАДАНИЙ абзац: «чутки про можливу відставку головнокомандувача ЗСУ
     Валерія Залужного, проте у Генштабі та в Офісі президента це спростовують».
     Звірка з оригіналом ([ТСН, 21.07 14:09](https://tsn.ua/politika/fedorov-vidmovyvsia-vid-usikh-posad-krim-odniyeyi-dzerela-bbc-rozkryly-detali-holovnoyi-kadrovoyi-intryhy-3132239.html)):
     Михайло (не «Вадим») Федоров відмовився від усіх запропонованих посад і
     розглядає лише повернення міністром оборони; у статті йдеться про конфлікт
     із **Сирським**; Залужного в ній немає, спростувань ніхто не давав.
  2. Новини, які ніколи не виходили в канал, хоча пройшли всі фільтри: 02–03.08
     так зникли удар по терміналу «Нової пошти» під Харковом (із загиблим) і
     стрілянина по військових ТЦК в Одесі.
- **Причина:** `curate_with_ai` просить модель відповідати «один рядок = одна
  подія, номери через кому», але парсер приймає **будь-який** рядок із цифр і
  ком як одну групу. Слабші моделі резервного ланцюга регулярно віддають один
  ПЛАСКИЙ список («3,7,1,9,2,5») — це природне прочитання інструкції «відповідай
  лише номерами». Парсер бачить одну «подію» з 6 новин, `merge_group` зливає їх
  в один пост і кладе всі URL-и в `item["group"]`. Далі:
  • сирі тексти чужих новин ідуть у промпт письменника як «ту саму подію описали
    й інші джерела» — модель змішує факти й додумує зв'язки (кейс 5200: до
    Федорова приклеїлась «Під Сирським захиталося крісло… все про можливу
    відставку», звідси й узялися «відставка головнокомандувача» та «спростування»);
  • після публікації ВСІ URL-и групи позначаються `published`, але з `msg_id`
    NULL — новина вважається висвітленою й більше ніколи не повернеться.
  Лексичний захист у проекті був (`merge_by_event`, поріг 0.5 + `_place_conflict`),
  але він працює ДО курації, а групи від курації йшли в `merge_group` без
  жодної перевірки.
- **Масштаб (published.db, 27.07–03.08):** 564 поглинутих новини за 7 діб
  (~60/добу); у 77% випадків поглинута новина не має з «постом-поглиначем»
  майже жодного спільного слова. 22 групи рівно по 6 елементів = `max_pick`,
  тобто прогони, де курація віддала плаский список і весь прогін схлопнувся
  в один пост замість трьох.
- **Рішення:** нова `split_unrelated_groups(items, groups)` — групи курації
  перевіряються лексикою ПЕРЕД `merge_group`. Базою беруть той самий елемент,
  що й `merge_group` (найдовший summary); у групі лишаються лише заголовки з
  Жаккаром по `_title_words` ≥ `GROUP_MIN_SIM` (0.15) і без конфлікту міст
  (`_place_conflict`). Решта стають окремими подіями одразу після своєї групи —
  тобто потрапляють у чергу, а не в небуття. Поріг 0.15 (а не 0.5, як у
  `merge_by_event`) підібрано на реальних даних: нижче — різні події, з 0.15
  починаються справжні пари («131 БПЛА» + «107 із 131»). Асиметрія навмисна:
  хибне розділення коштує один зайвий виклик LLM (подію дожене дедуп по
  згенерованому заголовку), хибне злиття — вигаданий пост і вбиту новину.
- **Верифіковано:** на кейсі 5200 Федоров і Сирський розділяються (схожість
  0.00); плаский список із 6 новин → 6 кандидатів; правильні злиття не
  ламаються (131/107 БПЛА, удар по Львову, витік хімії на Полтавщині, втрати
  за добу); прогін по всіх 564 злиттях за 7 діб — 435 новин (77%) було б
  врятовано; інтеграційний прогін `main()` з підміненими LLM і Telegram:
  1 група → 5 подій, 3 пости (стеля), жодного `published` без `msg_id`.
- **Побічний ефект (очікуваний):** обсяг постів зросте — частина прогонів
  зараз дає 1 пост замість 3 саме через хибне злиття. Орієнтовно 256/добу →
  до ~300/добу.
- **Залишок (не закрито):** 129 груп за 7 діб лишаються злитими, і частина з
  них помилкова через спільну «воєнну» лексику («Російський флот формує полки
  БПЛА» + «атакували 147 дронами», схожість 0.23). Лексика тут уперлася в
  стелю — потрібен фокусований семантичний чек. Винесено в наступну сесію.
- **Статус:** ✅ основне виправлено 2026-08-03 (`split_unrelated_groups`,
  `GROUP_MIN_SIM`); залишок по семантиці — окрема задача.

---

### БАГ-011 — відповідь моделі («надайте повний текст джерела») опублікована як новина
- **Дата:** 2026-08-03 (інцидент 2026-07-29). **Помітив:** власник.
- **Симптом:** 29.07, msg 7442 — у канал пішов текст: «Будь ласка, надайте
  повний текст джерела. У вашому повідомленні міститься лише заголовок та
  посилання, але немає самого тексту новини. Згідно з правилами, я не можу
  вигадувати факти…» — з нормальним підписом «📰 За даними: Радіо Свобода».
- **Причина:** дві складові.
  1. Джерело прийшло з фіда Google News, тому в записі був лише заголовок і
     redirect-посилання. `resolve_gnews_url` не розгорнув його, `fetch_article_text`
     заглушку Google прочитати не вміє → у моделі не було ЖОДНОГО тексту, і
     вона чесно відмовилась писати.
  2. Відмову ніхто не перевірив: у `main()` стояла рівно одна умова —
     `post_text.strip().upper().startswith("SKIP")`. Будь-яка інша відповідь
     вважалася готовим постом і йшла в Telegram. Тобто всі перевірки проекту
     стояли ДО генерації (фільтри, дедуп, курація) і жодної — ПІСЛЯ.
- **Рішення:** нова `is_bad_output(text)` — останній запобіжник перед каналом.
  Відсіває: мета-фрази моделі про себе чи про завдання (`_META_MARKERS`),
  службові позначки на початку рядка («Абзац 1:», «Заголовок:», «Para 2:»),
  `<think>`, ``` ``` ```, перший рядок довший за 160 символів (модель зліпила
  заголовок з текстом), пост коротший за 80 символів, менш ніж половина літер
  кирилицею (модель зірвалась в англійську). Повертає ПРИЧИНУ — вона йде в лог
  і в `skipped` з `reason='bad_output'`, а в підсумок адміну додано окремий
  рядок «🚫 Відсіяно як не-пост: N» (SKIP — редакційне рішення, брак — збій,
  плутати їх не можна).
- **Верифіковано:** ловить реальний текст msg 7442, `<think>`, код-блок,
  службові позначки, англомовний пост, обрубок, заголовок-стіну. Прогін по
  4346 РЕАЛЬНО опублікованих постах з бойової БД: відсіяно 7 (0,16%), і всі
  сім — справжній брак (msg 7442 + 6 постів, де модель не відділила заголовок
  від тексту). Хибних спрацювань — 0.
- **Статус:** ✅ виправлено 2026-08-03.

---

### БАГ-007 — Twitter/X 401 → закрито: X скасував безкоштовний доступ
- **Дата:** 2026-07-13→14. **Помітив:** лог (401) + Claude (перевірка порталу
  через браузер 2026-07-14).
- **Симптом:** кожна спроба твіту — 401 Unauthorized, 0 публікацій.
- **Причина (справжня):** спершу думали — права застосунку «Read only» / старі
  токени. Перевірка docs.x.com напряму показала інше: **X перевів увесь API v2
  на pay-per-use, безкоштовного tier більше немає.** 401 — наслідок вимкнення
  старого безкоштовного доступу, до якого прив'язані токени, а не прав.
  Тарифи: Post: Create — $0.015/твіт; **Post: Create (with URL) — $0.200/твіт**.
  Наш бот постить із посиланням → найдорожчий тариф (~$60/міс за 10 твітів/добу).
- **Рішення:** Twitter-напрямок **заморожено** (лишається `if: false` у
  воркфлоу) — платний доступ суперечить принципу «тільки безкоштовне, поки не
  заробляє». Код обмеження обсягу (MAX_TWEETS_PER_RUN, коміт dbd6bb8) лишається
  готовим на майбутнє. Альтернатива без витрат — Bluesky/Mastodon (безкоштовний
  API), якщо колись знадобиться присутність поза Telegram.
- **Статус:** ⏸ закрито як «заблоковано платним доступом X» (не код). Issue #2
  закрито (not planned). Повернутись — коли проект зароблятиме.

---

## ✅ Виправлені

### БАГ-010 — bot.py обрізаний: main() ніколи не викликався (канал мовчав)
- **Дата:** 2026-07-14. **Помітив:** Claude (аналіз коду за запитом власника).
- **Симптом:** з коміту `a74df05` (сесія 7) бот не публікував НІЧОГО — жодних
  постів, жодних помилок у логах чи підсумку адміну. Виглядало як «все ок,
  просто немає новин».
- **Причина:** той коміт (правки RSS_FEEDS) випадково обрізав хвіст `bot.py` —
  зник блок `notify_admin(summary)` + `conn.close()` + `if __name__ == "__main__":
  main()`. Файл лишався синтаксично валідним Python (обрізаний рядок був просто
  «висячим» виразом), тому `python bot.py` не падав з винятком — модуль просто
  визначав функції й завершувався, ніколи не викликаючи `main()`. Той самий клас
  інциденту, що вже описаний у сесії 7 як «Windows-маунт обрізає файли на
  кирилиці», тільки цього разу потрапило в git і пішло на прод.
- **Рішення:** відновлено видалений хвіст; заразом виправлено ще 2 суміжні баги
  в тому ж файлі, знайдені при читанні коду: `is_russian(title, title)` →
  `is_russian(title, summary)` (перевірка мови ігнорувала текст новини) і
  прибрано мертвий gate «чекати підтвердження теми» в `main()` (лічильник теми
  вже інкрементувався в `fetch_news()` до цієї перевірки, тож умова майже
  ніколи не спрацьовувала за задумом).
- **Верифіковано:** `ast.parse` на bot.py OK; `grep "__main__"` підтверджує
  рівно один виклик `main()`; закомічено й запушено (`1648278`). Через ~пів
  години після пушу база `published.db` отримала нові записи (11:03/11:15/11:31)
  — канал знову постить у проді.
- **Статус:** ✅ 2026-07-14.

### БАГ-009 — мало новин: третина RSS-джерел не працювала
- **Дата:** 2026-07-13→14. **Помітив:** власник (порожній канал) + Claude.
- **Симптом:** у каналі мало новин; feed_check «8 із 22 мертвих».
- **Причина (уточнена):** НЕ User-Agent і НЕ IP-блок (спершу так думали) — а
  ЗАСТАРІЛІ адреси RSS: 404 (Suspilne, УНІАН, Корреспондент, Mind, ОПОРА),
  «no feed by that name» (DW), протухлий api-код (radiosvoboda), 403 (LIGA,
  Економічна правда). Доведено скриптом feed_diag.py з IP раннера GitHub:
  робочі фіди тягнуться, «мертві» дають 404/403 — тобто запит доходить, шлях невірний.
- **Рішення:** parse_feed з браузерним UA (лишається, корисно); НОВИЙ feed_diag.py +
  воркфлоу для перевірки джерел з раннера; RSS_FEEDS переписано на 28 ПЕРЕВІРЕНИХ
  джерел (виправлено УНІАН/Suspilne правильними адресами; radiosvoboda/DW/Reuters/AP —
  через Google News site-фільтр; додано Цензор, LB, Європравду, Guardian, Al Jazeera,
  Euronews, CNN, Google News-агрегатор; прибрано непіднімані).
- **Статус:** ✅ 2026-07-14. Живучість усіх 28 підтверджена звітом feed_diag.

### БАГ-008 — Gemini 404 (мертва модель) + оманливе обрізання логу
- **Дата:** 2026-07-13. **Помітив:** власник (фідбек-бот) + Claude (читання коду).
- **Симптом:** у підсумку адміну — `Gemini: 404 Client Error: Not Found for url:
  https://generativelanguage.google`. Виглядало як поламаний/обрізаний домен.
- **Причина (двошарова):**
  1. URL у `bot.py` НЕ зламаний — повний і правильний. «...generativelanguage.google» —
     це рівно перші 70 символів тексту помилки, бо лог робив `str(e)[:70]`.
  2. Модель `gemini-2.5-flash` Є у ListModels (HTTP 200), але при генерації дає
     `404 NOT_FOUND: "no longer available to new users"` — Google закрив її для
     нових користувачів. Клас БАГ-001 (Cerebras 404 — модель недоступна акаунту).
- **Рішення:**
  • `call_llm`: додано гілку `status >= 400`, що логує тіло відповіді (`r.text`,
    ≤300 симв. консоль / ≤120 підсумок); обрізання в `except` 70→120. Саме це
    й показало точну причину.
  • `gemini_check.py`: перебирає кандидатів через РЕАЛЬНИЙ виклик генерації
    (присутність у ListModels ≠ доступність) і класифікує 200/404/503/429.
  • Модель: `bot.py` `gemini-2.5-flash` → **`gemini-3.5-flash`**. Діагностика
    власником підтвердила: 3.5-flash віддає 503 (не 404!) — тобто НАЗВА ВАЛІДНА,
    просто транзієнтне «high demand». `2.5-flash-lite` → 404 (теж закрита),
    `2.0-flash` → 429 (квота). Gemini — резервний провайдер (останній у ланцюзі),
    тож епізодичні 503/429 безпечні: `call_llm` просто не перепише цей пост і
    наступний запуск (щогодини) спробує знову. Ідемпотентно.
- **Статус:** ✅ 2026-07-13. Ретрай на 503 усередині `bot.py` навмисно НЕ
  додавали — Gemini резервний (останній у ланцюзі), fallback на інші провайдери
  + щогодинний цикл це покривають; зайва складність порушила б «мінімум зламів».
