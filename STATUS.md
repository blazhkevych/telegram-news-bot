# UA News — Стан проекту (STATUS.md)

> Читається на початку кожної сесії. Оновлюється в кінці. Не видаляти!

**Останнє оновлення:** 2026-07-13, 1-а сесія (Cowork, [OPUS]) — старт роботи за
схемою: додано інструкції проекту, ROADMAP; Z-2 (модель `bot.py` → 70b) + Z-1
(промпт проти фактичних помилок) + Z-3 (прибрано спам статистикою втрат).

---

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а — [OPUS]) — старт за схемою LexControl.
> ЗАЧІПАЄ: `bot.py`, `war_stats.py`; нові `STATUS.md`, `ROADMAP.md`.
> НАСЛІДКИ ЗАПУСКУ: нових secret НЕ треба (усе на наявному `GROQ_API_KEY`);
> у `published.db` при першому запуску сама з'явиться таблиця `war_stats_log`.
>
> ЩО ЗРОБЛЕНО:
> • Z-2 (bot.py): головна модель переписування `llama-3.1-8b-instant` →
>   `llama-3.3-70b-versatile` (безкоштовна, той самий ключ, вже вживається у
>   twitter_bot.py); temperature 0.6 → 0.4 (точність). Обробку `429`/фолбек
>   лишено як є.
> • Z-1 (bot.py, `rewrite_with_ai`): у промпт додано блок «ТОЧНІСТЬ» — не
>   вигадувати стать/факти, зберігати імена як у джерелі, не лити воду, при
>   браку інформації писати коротше. Ціль — прибрати помилки типу «звільнив
>   уряд Свириденка» (Свириденко — жінка) + порожні пости-повтори.
> • Z-3 (war_stats.py): прибрано спам. Раніше scheduler запускав статистику
>   втрат ~17 разів/добу → та сама картинка постилась знову й знову. Додано
>   дедуп у `published.db` (таблиця `war_stats_log`): не постити двічі за день
>   і не постити ту саму картинку повторно; `mark_posted` лише після успіху.
>
> ВЕРИФІКОВАНО:
> • war_stats.py — `py_compile` OK; логіка дедупу — 5/5 на РЕАЛЬНІЙ копії
>   published.db (перший запуск / повтор того ж дня / інша картинка того ж дня /
>   новий день з новою / новий день зі старою картинкою).
> • bot.py — джерело правди Read/хост: файл цілий (308 рядків, коректний кінець
>   `main()`), обидві правки well-formed (f-string закритий, json з новою
>   моделлю й 0.4). Повний `py_compile` у пісочниці впав через ОБРІЗАННЯ mount
>   (файл видно лише до 282-го рядка, обрив на середині слова) — відомий
>   артефакт, не помилка коду.
>
> КРОКИ ВЛАСНИКА:
> 1) Закомітити: `bot.py`, `war_stats.py`, `STATUS.md`, `ROADMAP.md`
>    (git add/commit/push).
> 2) Перевірити вручну: GitHub → Actions → «News Bot» → Run workflow. У логах
>    новина має переписуватись якісніше; статистика втрат — максимум 1 раз/добу.
> 3) Кілька наступних постів у каналі проглянути на точність (імена/стать,
>    без «води»).
>
> ⏭ НАСТУПНА СЕСІЯ: Z-5 (новий закріп — вже замінено власником, лишилось звірити
> опис каналу) або Z-2b [FABLE] (ланцюг безкоштовних провайдерів-фолбеків:
> Gemini Flash за наявним GEMINI_API_KEY + Cerebras 1M токенів/добу — резервування
> й ще краща якість). Далі за пріоритетом — Z-6 (тривоги), Z-7 (курс НБУ).
