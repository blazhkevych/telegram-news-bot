# UA News — Стан проекту (STATUS.md)

> Читається на початку кожної сесії. Оновлюється в кінці. Не видаляти!

> ✅ ЦЯ СЕСІЯ (2026-07-16, 10-а — [Code/Fable]) — ревізія за добу: інфраструктура здорова, знайдено корінь дублів-перефразів (#4) і погоди (#3).
> ЗАЧІПАЄ: bot.py (курація+дедуп), digest.py (погода, parse_mode). НАСЛІДКИ ЗАПУСКУ: нових secret нема; поведінка — менше дублів, погода по всіх містах.
>
> РЕВІЗІЯ ЗА ДОБУ (15.07 11:00 → 16.07 11:00):
> • Інфраструктура: усі ~100 прогонів зелені (зовн. тригер кожні 15 хв працює), 28/28 фідів живі,
>   war_stats/дайджести/feed_check відпрацювали щодня (daily_log повний). БД зберігається (pull --rebase діє).
> • ОБСЯГ: 317 постів/добу (~13/год) — бот постить 3/прогін майже щоразу. Це МАКСИМУМ конвеєра;
>   чи не забагато для підписників — рішення власника (див. «ПИТАННЯ ВЛАСНИКУ»).
> • ДУБЛІ (#4) підтверджено масово: «ЦРУ: 20-30 хв» ×3, «21-й пакет санкцій» ×7 варіацій, Мі-28 ×3,
>   «Посли ЄС захист українців» ×5; той САМИЙ заголовок заходив повторно через Google News (інший URL).
>
> ЗНАЙДЕНІ КОРЕНІ І ФІКСИ (закомічено f15689b, 600c988; перевірено бойовим прогоном 29484187129 ✅):
> • 🔴 Курація ЗАВЖДИ йшла на найслабшій gemma-31b: max_tokens=400 спалювався reasoning'ом gpt-oss-120b
>   → finish_reason=length → Groq відпадав. У логах це видно як «обірвано» ПЕРЕД кожною курацією. Тепер 1600.
> • 🔴 Вікно дедупу курації: LIMIT 25 заголовків = ~2 год історії при 300/добу — ранкові події ввечері
>   «невидимі». Тепер 60. + Лексичний is_duplicate_title повернуто і на AI-шлях (хибних злиттів міст
>   не буде — _place_conflict блокує; страхує від повторів через Google News).
> • 🔴 Погода (#3): корінь — НЕ 429, а ReadTimeout на серії з 29 послідовних запитів. Тепер ОДИН
>   batch-запит (координати через кому) — перевірено живим викликом: 29/29 міст з даними.
> • digest.py: прибрано parse_mode=Markdown (текст пише LLM; випадкові _ * [ валили б увесь дайджест).
> ⚠ Groq майже весь день у 429 (добовий ліміт з'їдають ~300 переписувань) → і письмо, і курація на gemma;
>   звідси одруки в постах («дешею», «всіій»). Зменшення обсягу → менше викликів → частіше сильна модель.
>
> 📋 ПИТАННЯ ВЛАСНИКУ: (а) 317 постів/добу — лишити чи знизити (напр. MAX_POSTS_PER_RUN 3→2 або
>   поріг важливості в курації)? Це і якість підніме (менше палимо ліміт Groq). (б) У закріпленому
>   пості досі рядок «🐦 Ми в X: @LiveUkrNews» — Twitter заморожено, варто прибрати (пункт з сесії 9).
>
> ДОДАНО ПІЗНІШЕ ЦІЄЇ Ж СЕСІЇ (рішення власника: «якість — так, але ліміти не повинні простоювати»):
> • 🧠 Розумний розподіл лімітів (коміт dd47976, call_llm save_strong): обсяг НЕ ріжемо. Курація і
>   ПЕРШИЙ (найважливіший) пост прогону → Groq(120B)→Cerebras→Gemini; 2-й/3-й пости → Cerebras→Gemini→Groq.
>   Groq тепер ~200 викликів/добу замість ~400 (не вигорає зранку), ліміти Cerebras/Gemini працюють.
> • Перевірено прогоном 29486172622 ✅: 3 пости, і лексичний запобіжник УПЕРШЕ спрацював у бою —
>   «Британія розробить балістичні ракети» відсіяно як дубль (постили о 11:15 з іншим URL).
>
> ⚠ ІНЦИДЕНТ (обрив живлення 16.07 ~11:54, під час git pull --rebase): NTFS занулив щойно записані
>   файли — .git/HEAD, index, refs/remotes/origin/*, ORIG_HEAD/FETCH_HEAD/COMMIT_EDITMSG і САМ bot.py
>   (52КБ суцільних NUL). ВРЯТУВАЛО те, що коміт встиг створитись: об'єкти git уціліли, bot.py відновлено
>   `git checkout -- bot.py`. РЕЦЕПТ на повтор: HEAD → `ref: refs/heads/main`; rm битих index/refs/rebase-merge;
>   git fetch + remote set-head; git fsck; робочі файли перевіряти на NUL-байти (grep може NUL не показати —
>   надійніше python: src.count(b'\\x00')). Комітити ПЕРЕД pull — незакомічене було б втрачено назавжди.
>
> ⏭ НАСТУПНА СЕСІЯ: (а) подивитись дублі за добу ПІСЛЯ фіксів (той самий SQL по published + t.me/s);
>   (б) ранковий дайджест 17.07 — чи всі міста з погодою (закрити issue #3); (в) якщо дублі лишаються —
>   давати курації повні 24 год опублікованого стисло (кластеризувати перед промптом), issue #4;
>   (г) у підсумках адміна стежити за балансом «Groq×N, Cerebras×M, Gemini×K» — чи розтягся Groq на день.

> ✅ ЦЯ СЕСІЯ (2026-07-15, 9-а — [Code/Opus]) — канал ожив: знайдено корінь мовчання, ШІ-курація, джерела.
> ЗАЧІПАЄ: bot.py (головне), digest.py, scheduler.py, war_stats.py, twitter_bot.py, news_bot.yml, BUGS.md, ROADMAP.md.
> НАСЛІДКИ ЗАПУСКУ: нових secret немає; Twitter лишається вимкненим; поведінка каналу помітно змінилась (див. нижче).
>
> ЩО ЗРОБЛЕНО (у порядку важливості):
> • 🔴 КОРІНЬ «МОВЧАННЯ КАНАЛУ» (БАГ-010): коміт a74df05 обрізав хвіст bot.py — зник виклик
>   `if __name__ == "__main__": main()`. Файл лишався валідним Python, помилок не було — бот просто
>   нічого не робив. Відновлено; канал одразу почав постити.
> • 🔴 ВТРАТА СТАНУ (корінь дублів + 429): крок «Save database» робив git push БЕЗ pull --rebase → щойно
>   в main з'являвся чужий коміт, push падав і published.db НЕ зберігалась. Бот годинами працював зі старою
>   БД: не знав про опубліковане (→ дублі) і про SKIP (→ ті самі новини знову в LLM → 429 на всіх
>   провайдерах). Тепер: pull --rebase + retry×3 + явна помилка в лог.
> • 🧠 ШІ-КУРАЦІЯ (заміна лексичних алгоритмів — ідея власника): один виклик LLM бачить опубліковане за добу
>   + усіх кандидатів, повертає лише номери (рядок = подія, номери = джерела про неї). Дедуп, злиття джерел,
>   вибір важливого — усе одним викликом. 12 викликів → 4. Fallback на лексичний шлях при 429/смітті.
> • 📰 ДЖЕРЕЛА: «За даними: X, Y» — кожне джерело ПРЯМЕ посилання на свою статтю. «Читати повністю» прибрано
>   (дублював перше джерело). Назви задані ЯВНО в RSS_FEEDS (feed.title довільний: Укрінформ віддає «Останні
>   новини», Цензор — «Цензор.НЕТ - Новини», Європравда — нічого).
> • 🌍 СВІТОВІ НОВИНИ не потрапляли в канал: ukraine_score виштовхував en-джерела за межі топ-12 ще до курації.
>   Тепер резерв: 9 укр + 3 світові. Перевірено — Reuters з'явився в каналі.
> • 🎨 СТИЛЬ NV + HTML: жирний заголовок з доречним емодзі + короткі абзаци; parse_mode Markdown→HTML
>   (Markdown валився на символах _ * [ у сирому тексті новини).
> • ✂️ ОБІРВАНІ ПОСТИ («...дворічну підтрим»): gpt-oss-120b — reasoning-модель, палила max_tokens на
>   «міркування». Тепер call_llm відкидає finish_reason=length і віддає наступному провайдеру; max_tokens 900→1600.
> • SKIP-кеш (таблиця skipped) — відхилені новини не ганяються через LLM повторно.
> • scheduler/digest/war_stats: mark_today лише при успішному коді виходу (дайджест більше не «губиться» на добу).
> • Twitter ЗАМОРОЖЕНО остаточно: X скасував безкоштовний API (pay-per-use, $0.20/твіт з посиланням).
>
> ⚠️ МОЇ ПОМИЛКИ ЦІЄЇ СЕСІЇ (щоб не повторити):
> • Додав у промпт «краще SKIP, ніж порожній однорядок» → модель почала відхиляти ~70% новин. Поки бот
>   перебирав 12 кандидатів, це маскувалось; після курації (пул 3) канал видав 0 постів. Прибрано.
> • Змінив структуру sources на {name,url} і пропустив одне місце → TypeError → бот падав, не публікуючи.
>   Знайдено власним прогоном ДО того, як побачив власник.
> • ВИСНОВОК (вимога власника, тримати завжди): ПЕРЕВІРЯТИ на реальних логах/каналі ПЕРЕД тим, як казати
>   «працює» і перед наступною зміною. Юніт-тести чистих функцій НЕ ловлять регресій промпту/LLM.
>
> 🛠 ЯК ПЕРЕВІРЯТИ (ключове для наступних сесій):
> • Логи прогонів: `gh run list --repo blazhkevych/telegram-news-bot`, `gh run view <id> --log` → грепати
>   маркери: 📥 Знайдено / 🧠 Курація AI / ⏭ AI пропустив / 🏁 Опубліковано / Traceback.
> • Ручний запуск: `gh workflow run news_bot.yml --ref main` (крон GitHub часто затягує).
> • Канал видно самому: браузер → https://t.me/s/uanewsbyai (старі записи про «пісочниця не має мережі» — неактуальні).
> • gh CLI встановлено (winget). Токен git-credential має scope repo/workflow, але БЕЗ read:org, тому
>   `gh auth login` не проходить — працює лише інлайн: `GH_TOKEN=<токен> gh <команда>`.
>
> 📋 ПРОЦЕС: відкриті задачі — у GitHub Issues (#1 якість переказу, #3 погода, #4 дублі-перефрази + перекіс).
>   BUGS.md — архів виправленого. ROADMAP.md — стратегія. STATUS.md — цей журнал.
>
> ⏭ НАСТУПНА СЕСІЯ:
> (а) ДУБЛІ-ПЕРЕФРАЗИ (issue #4) — головне невирішене: курація ловить не все («Посли ЄС» вийшли двічі).
> (б) Перевірити, чи модель тепер зберігає точні назви (орден/номер закону) — промпт оновлено, результат не бачив.
> (в) Погода (issue #3) — діагностику додано, чекаємо лог ранкового дайджесту (чи це 429 Open-Meteo).
> (г) Власнику: у закріпленому пості прибрати рядок «🐦 Ми в X: @LiveUkrNews» — Twitter заморожено.
> (д) Нюанс: посилання на Reuters/AP ведуть через news.google.com (редірект), бо їх тягнемо через Google News.

> ✅ ЦЯ СЕСІЯ (2026-07-14, 8-а — [Code/Sonnet]) — критичний фікс bot.py (main() не викликався) + дрібні баги; GitHub тепер веду сам.
> ЗАЧІПАЄ: bot.py, digest.py, scheduler.py, war_stats.py, twitter_bot.py, BUGS.md, ROADMAP.md.
> НАСЛІДКИ ЗАПУСКУ: нових secret немає; наступний прогін cron має знову почати постити.
> ЩО ЗРОБЛЕНО:
> • КРИТИЧНО: коміт a74df05 (сесія 7) обрізав хвіст bot.py — зник виклик
>   `if __name__ == "__main__": main()`. Файл валідний синтаксично (не падав з помилкою),
>   тому просто виконувався і одразу завершувався, НІЧОГО не роблячи — жодних постів,
>   жодних помилок у логах. Це, найімовірніше, справжня причина «порожнього каналу»
>   з 14.07 (а не лише застарілі RSS-адреси з БАГ-009). Відновлено повністю, БАГ-010.
> • bot.py: `is_russian(title, title)` → `is_russian(title, summary)` — фільтр
>   російської мови раніше не перевіряв текст новини, лише заголовок (двічі).
> • bot.py: прибрано мертвий gate «чекати підтвердження теми» (`get_topic_count < 1`
>   у main()) — лічильник теми інкрементувався ще в fetch_news() до цієї перевірки,
>   тож умова спрацьовувала лише для заголовків без слів довших 5 символів.
> • scheduler.py + digest.py/war_stats.py: `mark_today()` тепер лише при успішному
>   коді виходу скрипта (раніше дайджест/статистика «губились» на добу при падінні —
>   саме той пункт, що висів у «НАСТУПНА СЕСІЯ» з сесії 6/7).
> • twitter_bot.py: додано браузерний UA для RSS (як у bot.py) — окрема копія, бо
>   імпорт з bot.py впав би через відсутні TELEGRAM_* у кроці Twitter-воркфлоу.
> • bot.py: `is_valid_image` — fallback на GET, якщо CDN блокує HEAD (403/405).
> • ROADMAP.md: Z-3 (спам war_stats) і Z-4 (дедуп подій) позначено як фактично вже
>   реалізовані в коді (daily_log guard + is_duplicate_title Жаккар) — ROADMAP не
>   встигли оновити раніше.
> ВЕРИФІКОВАНО: ast.parse на всіх 5 змінених файлів OK; grep підтвердив рівно один
>   виклик `main()` під `__main__` у кожному з bot.py/digest.py/war_stats.py;
>   git diff перед комітом перевірено вручну рядок-в-рядок.
> ⚙ GITHUB (нове, важливо для наступних сесій): у режимі "Code" підтверджено —
>   `git push` працює напряму (credential manager уже авторизований під власником,
>   `origin/main` == локальний HEAD після push). Додатково встановлено `gh` CLI
>   (winget, 2.96.0): `gh auth login --with-token` не проходить (токен git-credential
>   без scope `read:org`, а gh на цьому вимагає його при логіні) — ОБХІД: токен
>   передається як `GH_TOKEN=... gh <команда>` інлайн у кожному виклику (сам токен
>   ніде не друкується і не лежить на диску). Реальні scope токена: `gist, repo,
>   workflow` — issues/PR/workflow дії доступні. ПРЕФЕРЕНЦІЮ (1) нижче оновлено:
>   власник підтвердив — Claude в режимі "Code" веде git/GitHub самостійно.
> 🔎 БАЧУ КАНАЛ САМ (важливо): у режимі "Code" через вбудований браузер
>   (preview_start → https://t.me/s/uanewsbyai → get_page_text) видно РЕАЛЬНІ свіжі пости.
>   Старі записи «пісочниця не має мережі, верифікує власник / t.me/s кешується» — НЕ стосуються
>   браузера Claude Code. Тепер сам перевіряю вигляд/якість постів після публікації.
> 🎨 СТИЛЬ NV + HTML (зроблено, коміти e3172d0, 3ff7140): пости тепер — жирний заголовок з доречним
>   емодзі + короткі абзаци; parse_mode Markdown→HTML (надійніше, не валиться на _ * [ у тексті).
>   Формат підтверджено на реальному каналі (пости від 17:01 ✅). Далі підкручено промпт: повніші пости
>   (2-3 змістовні абзаци, SKIP замість однорядка), доречні за тоном емодзі, заборона «води».
> 🐦 TWITTER/X — ЗАМОРОЖЕНО ОСТАТОЧНО (рішення власника 2026-07-14): X скасував безкоштовний API,
>   тепер pay-per-use ($0.015/твіт, $0.20/твіт з посиланням — а бот постить з посиланням). 401 був
>   наслідком вимкнення free-tier, не прав. Суперечить «тільки безкоштовне» → лишили if:false. BUGS-007
>   закрито, issue #2 закрито (not planned). Код обмеження обсягу (MAX_TWEETS_PER_RUN) готовий на майбутнє.
>   Безкоштовна альтернатива на потім — Bluesky/Mastodon. ⚠ У ЗАКРІПЛЕНОМУ ПОСТІ прибрати рядок «🐦 Ми в X:
>   @LiveUkrNews» (обіцяє те, чого нема) — власник постить сам.
> 📋 ПРОЦЕС (нове): відкриті баги/задачі тепер трекаємо в GitHub Issues, НЕ у BUGS.md.
>   Створено issues #1 (БАГ-006 якість), #2 (БАГ-007 Twitter 401), #3 (погода недоступна).
>   BUGS.md перетворено на АРХІВ виправленого (детальні розбори закритих багів). ROADMAP.md —
>   стратегія/пріоритети. STATUS.md — журнал сесій і моя пам'ять між сесіями (лишається головним).
>   Коли закриваєш баг: перенеси розбір у BUGS.md «✅ Виправлені» + закрий issue комітом (closes #N).
> ⏭ НАСТУПНА СЕСІЯ: підтвердити з фідбек-бота, що канал знову постить (перевірити
>   підсумок після найближчого cron-прогону); далі — issue #1 (якість переказу) або
>   issue #3 (погода) — обидва невеликі, дають видимий результат.

> ✅ ЦЯ СЕСІЯ (2026-07-14, 7-а — [OPUS]) — джерела виправлено й розширено (28 RSS); зовн. тригер; тривоги відкладено.
> ЗАЧІПАЄ: bot.py (RSS_FEEDS), feed_diag.py (новий), .github/workflows/feed_diag.yml (новий), EXTERNAL_TRIGGER.md.
> НАСЛІДКИ ЗАПУСКУ: додано зовн. планувальник cron-job.org (діє); нових secret у боті нема.
> ЩО ЗРОБЛЕНО:
> • ДІАГНОЗ уточнено: «мертві» фіди — це НЕ User-Agent і НЕ IP-блок (спростовано через feed_diag на раннері GitHub),
>   а просто ЗАСТАРІЛІ адреси (404/403). БАГ-009 переформульовано й закрито.
> • Новий feed_diag.py + workflow_dispatch-воркфлоу feed_diag.yml: тестує джерела З IP сервера GitHub і шле звіт
>   адміну (HTTP-статус / к-сть записів / причина). Два раунди кандидатів прогнано власником.
> • RSS_FEEDS переписано: 28 ПЕРЕВІРЕНИХ джерел (17 uk / 11 en). Виправлено УНІАН (rss.unian.net/site/news_ukr.rss),
>   Suspilne (/rss/all.rss). Додано: Цензор, LB.ua, Європравда, Google News (укр агрегатор), Guardian, Al Jazeera,
>   Euronews, CNN, і через Google News (site-фільтр) — Радіо Свобода, DW, Reuters, AP. Прибрано мертві:
>   Корреспондент, Mind, ОПОРА, LIGA, Економічна правда, стара radiosvoboda/DW, Громадське/Главком, ZN.ua.
> • Зовнішній тригер (з сесії 6) ДІЄ: cron-job.org → GitHub API workflow_dispatch, TEST RUN = 204, нові прогони йдуть.
> ВЕРИФІКОВАНО: py_compile bot.py+feed_diag.py; import bot → 28 джерел, усі мають url+lang; diag_one прогнано на моках
>   (200/403/timeout/connerr/200-без-RSS); живучість усіх 28 підтверджена звітом feed_diag власника (усі ✅).
> ⚠ ІНЦИДЕНТ (важливо для наступних сесій): цей Windows-маунт КІЛЬКА разів обрізав файли на кирилиці —
>   і Edit-tool, і один точковий Python-replace (feed_diag втратив виклик main → порожній прогон). МІТИГАЦІЯ, якої
>   тримаюсь: писати файли повним перезаписом через bash-Python (newline='\n'), і ПЕРЕВІРЯТИ з диску ОКРЕМОЮ
>   командою (wc -l + grep кінцевих рядків + py_compile), а не покладатись на успіх запису.
> ⚠ ТРИВОГИ — відкладено. Рішення узгоджено: миттєвість обов'язкова → потрібен always-on хост (GitHub пакетний,
>   ~1–2 хв затримки — не годиться). Безкоштовний шлях без картки = старий Android + Termux, увімкнений у зарядку 24/7
>   (опитувати alerts.in.ua кожні ~10–15 с). У власника зараз нема запасного телефону — ставимо на паузу.
> ⚠ ПРЕФЕРЕНЦІЇ ВЛАСНИКА (тримати завжди): (1) [ОНОВЛЕНО 2026-07-14, сесія 8] У режимі
>   "Code" Claude веде git/GitHub САМОСТІЙНО (commit/push/issues) — окреме підтвердження
>   не потрібне щоразу. У Cowork-чаті (де немає прямого доступу до диска/git) лишається
>   старий режим — текст коміту, пушить власник. (2) Логи приймати ФАЙЛОМ (досить одного — вивід кроку діагностики), не копіпастом. (3) Працювати
>   як команда, приймати потрібну експертну роль. (4) НЕ піднімати тему грошей/платних рішень, поки проект не заробить.
>   (5) Пояснювати простими словами, без жаргону; вести STATUS.md щосесії (як у LexControl).
> КРОКИ ВЛАСНИКА: 1) Переконатись, що запушено останні bot.py (28 джерел) і feed_diag.py (коміт-меседжі я давав у чаті).
>   2) Більше нічого — джерела працюють автоматично.
> ⏭ НАСТУПНА СЕСІЯ (новий чат): (а) СТИЛЬ постів у дусі NV — заголовок+емодзі, короткі абзаци, цитата-blockquote з
>   атрибуцією + перехід parse_mode Markdown→HTML з безпечним екрануванням (приклади постів NV власник надсилав).
>   (б) Тривоги — коли з'явиться запасний Android (Termux + alerts.in.ua, безкоштовний токен). (в) Опційно: вимкнути
>   вбудований cron */15 у news_bot.yml (зовн. тригер тепер основний; concurrency робить співіснування безпечним).
>   (г) Спостерігати: digest.py mark_today("morning") при падінні губить дайджест на добу; погода «дані недоступні»
>   для ~10 міст (ліміт Open-Meteo?); БАГ-006 (якість переказу), БАГ-007 (Twitter 401).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 6-а — [OPUS]) — корінь «порожнього каналу»: GitHub дропає крон */15.
> ЗАЧІПАЄ: EXTERNAL_TRIGGER.md (новий). Код/воркфлоу НЕ чіпав.  НАСЛІДКИ ЗАПУСКУ: новий зовн. планувальник + PAT (кроки нижче).
> ЩО ЗРОБЛЕНО:
> • Власник підтвердив: у Actions є Scheduled-запуски, банера про disable нема — крон УВІМКНЕНИЙ. Але
>   реальна каденція сьогодні: 00:42→01:40→02:47→05:11→09:09→13:18→16:15 — діри по 3–4 год. GitHub
>   деприоритезує/дропає високочастотні крони (`*/15`). Звідси мало новин і «сповзання» дайджестів.
> • Пояснено «Доброго ранку!» о 19:00: ранковий дайджест виходить на першому запуску за добу у вікні
>   6:00–21:00; через діри перший вдалий припав на вечір. Легіт-функції (war_stats + digest) — не баг.
> • РІШЕННЯ (вибір власника): зовнішній тригер cron-job.org → GitHub API `workflow_dispatch` щопівгодини.
>   Воркфлоу вже має `workflow_dispatch` + `concurrency` (накладання безпечне), тож код не міняв.
>   Написано EXTERNAL_TRIGGER.md — PAT (Actions RW), налаштування cron-job.org, перевірка (HTTP 204), безпека токена.
> ВЕРИФІКОВАНО: назва воркфлоу news_bot.yml і наявність workflow_dispatch перевірені в файлі; побудований
>   dispatch-URL/заголовки/боді — за поточним GitHub REST (api.github.com/.../workflows/news_bot.yml/dispatches, {"ref":"main"}).
>   Живий тест dispatch робить власник (потрібен PAT — секрет).
> КРОКИ ВЛАСНИКА:
>   1) Виконати EXTERNAL_TRIGGER.md: створити fine-grained PAT (repo telegram-news-bot, Actions: Read and write),
>      завести завдання на cron-job.org (POST на dispatch-URL, 3 заголовки, body {"ref":"main"}), Test run → чекати HTTP 204.
>   2) Переконатись у GitHub → Actions, що з'явився прогон з тригером workflow_dispatch, і у фідбеку — підсумок.
>   3) Закомітити доки: git add EXTERNAL_TRIGGER.md STATUS.md BUGS.md && git commit -m "Зовнішній тригер: обхід дропання крона GitHub" && git push.
>      (Токен НЕ комітити — він лише в cron-job.org.)
> ⏭ НАСТУПНА СЕСІЯ: (а) `digest.py` — mark_today("morning") при падінні digest губить дайджест на добу; (б) погода
>   «дані недоступні» для ~10 міст (ліміт Open-Meteo?); (в) підтвердити оживлення RSS-фідів (БАГ-009); БАГ-006, БАГ-007.

> ✅ ЦЯ СЕСІЯ (2026-07-13, 5-а — [OPUS]) — «мало новин»: 8/22 фідів «мертві» = блокування User-Agent.
> ЗАЧІПАЄ: bot.py (новий parse_feed), feed_check.py.  НАСЛІДКИ ЗАПУСКУ: нічого (адитивно; secret/cron без змін).
> ЩО ЗРОБЛЕНО:
> • Діагноз: канал живий (фідбек «опубліковано 3 з 12»), проблема — низький обсяг. feedparser.parse(url)
>   шле власний UA `feedparser/6.0.12`, який ріжуть UNIAN/Suspilne/DW/LIGA/Mind/Korrespondent → порожньо → фолс-«dead».
> • bot.py: додано `parse_feed(url)` — тягне через requests з браузерним UA (FEED_UA), байти → feedparser;
>   fallback на прямий feedparser.parse при винятку/HTTP≠200/порожньо. `fetch_news` тепер кличе parse_feed.
> • feed_check.py: імпортує і використовує той самий parse_feed (єдина логіка UA).
> • «Битий Markdown» у звіті RSS — НЕ баг: notify_admin шле без parse_mode (чистий текст); злиплі лінки —
>   артефакт копіювання з Telegram-клієнта. Код не чіпав.
> ВЕРИФІКОВАНО: ast.parse bot.py+feed_check.py OK; parse_feed на моках — 200→1 запис, виняток→fallback,
>   403→fallback (усі PASS). Реальні фіди з пісочниці недоступні (проксі) — оживлення перевіряє власник (крок 3).
> ⚠ ІНЦИДЕНТ: перша спроба Edit обрізала bot.py і feed_check.py посеред кирилиці (CRLF/UTF-8). Відновлено з
>   `git show HEAD:<файл>`, патч застосовано Python-байтами. Обидва файли цілі й валідні (ast OK).
> ⚠ GIT: `.git/index` занулено + висить порожній `.git/index.lock` (не зняти з пісочниці). Лагодиться в кроці 1.
> КРОКИ ВЛАСНИКА:
>   1) Полагодити індекс (у папці проекту, cmd): `del .git\index.lock` → `del .git\index` → `git reset` → `git status`
>      (покаже змінені bot.py, feed_check.py, STATUS.md, BUGS.md; робочі файли не постраждають).
>   2) `git add bot.py feed_check.py STATUS.md BUGS.md`
>      `git commit -m "RSS: браузерний User-Agent (parse_feed) — оживити заблоковані фіди"`
>      `git pull --rebase` (підтягнути 9579c5b — оновлення published.db воркфлоу) → `git push`.
>   3) Запустити воркфлоу (workflow_dispatch), глянути фідбек «🩺 Перевірка RSS»: мертвих має стати помітно менше
>      (з 8 ближче до 2). Якщо suspilne/unian/dw/liga/mind/korrespondent ожили — фікс спрацював.
>   4) Якщо radiosvoboda (/api/zrqiteoiqp) і oporaua (/feed) лишаться мертвими — вони справді протухли; скажи, підберу заміну.
> ⏭ НАСТУПНА СЕСІЯ: за звітом власника — прибрати/замінити реально мертві radiosvoboda/oporaua; БАГ-006 (якість), БАГ-007 (Twitter 401).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 2-а — [OPUS]) — Gemini 404: справжня причина vs оманливий лог.
> ЗАЧІПАЄ: bot.py, gemini_check.py (новий), BUGS.md.  НАСЛІДКИ ЗАПУСКУ: нічого (адитивно, без нових secret/cron).
> ЩО ЗРОБЛЕНО:
> • Розібрано хибну тривогу: URL Gemini у bot.py:78 цілий і правильний. «...generativelanguage.google»
>   у підсумку адміну — це рівно 70 перших символів помилки, бо лог робив `str(e)[:70]`. Домен не зламаний.
> • `call_llm`: додано гілку `status >= 400`, що логує ТІЛО відповіді Google (справжня причина 404) —
>   до 300 симв. у консоль, до 120 у підсумок адміну; обрізання в `except` розширено 70→120.
> • Новий `gemini_check.py` — окрема діагностика (список моделей + тест-виклик), не чіпає бойовий bot.py.
> • БАГ-008 у BUGS.md (двошарова причина: обрізання логу + справжній 404 з боку Google).
> ВЕРИФІКОВАНО: py_compile bot.py і gemini_check.py; логіку нової гілки прогнано на фейкових відповідях
>   (429 → «ліміт», 404 з тілом → причина в логу, 200 → контент повертається). Бойовий ключ не смикав.
> КРОКИ ВЛАСНИКА: 1) `git add bot.py gemini_check.py BUGS.md STATUS.md && git commit && git push`.
>   2) Локально запустити `GEMINI_API_KEY=... python gemini_check.py` → глянути, модель чи API винні.
>   3) За результатом: виправити назву моделі в bot.py:80 АБО ввімкнути Generative Language API. Це роблю я? — скажи.
> ⏭ НАСТУПНА СЕСІЯ: за виводом gemini_check закрити БАГ-008 (модель/ключ); поспостерігати БАГ-006 (якість).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 3-я — [OPUS]) — БАГ-008: причина знайдена, модель замінено.
> ЗАЧІПАЄ: bot.py (рядок 80), gemini_check.py, BUGS.md.  НАСЛІДКИ ЗАПУСКУ: нічого (заміна назви моделі; secret/cron без змін).
> ЩО ЗРОБЛЕНО:
> • Власник прогнав gemini_check.py з бойовим ключем. Діагноз точний: ListModels → HTTP 200,
>   `gemini-2.5-flash` У СПИСКУ Є, але генерація дає 404 «This model ... is no longer available to
>   new users. Please update your code to use a newer model.» Тобто Google закрив модель для нових
>   користувачів — не URL, не ключ, не вимкнений API. Клас БАГ-001.
> • bot.py:80 `gemini-2.5-flash` → `gemini-3.5-flash` (актуальна GA зі списку акаунта).
> • gemini_check.py: п.2 тепер перебирає кандидатів (3.5-flash, 2.5-flash-lite, 2.0-flash,
>   flash-latest) через РЕАЛЬНИЙ виклик генерації і друкує, які 200. Присутність у ListModels
>   не гарантує доступності — цей баг саме це й показав.
> ВЕРИФІКОВАНО: py_compile gemini_check.py OK; bot.py — заміна одного рядка-моделі (решта не чіпалась).
>   Бойовий Gemini-виклик робить власник (крок нижче).
> КРОКИ ВЛАСНИКА: 1) Ще раз `python gemini_check.py` — переконатись, що рядок `gemini-3.5-flash: HTTP 200`
>   і в підсумку «✅ робоча». Якщо ні — у виводі буде підказка, на що замінити (перший зелений кандидат),
>   скажи мені або постав його сам у bot.py:80. 2) `git add bot.py gemini_check.py BUGS.md STATUS.md &&
>   git commit -m "БАГ-008: Gemini модель 2.5-flash→3.5-flash + лог тіла помилки" && git push`.
> ⏭ НАСТУПНА СЕСІЯ: як власник підтвердить 200 на 3.5-flash — закрити БАГ-008 у ✅; далі БАГ-006 (якість),
>   БАГ-007 (Twitter 401).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 4-а — [OPUS]) — БАГ-008 ЗАКРИТО: gemini-3.5-flash підтверджено валідною.
> ЗАЧІПАЄ: gemini_check.py (класифікація статусів), BUGS.md (БАГ-008 → Виправлені).  НАСЛІДКИ ЗАПУСКУ: нічого.
> ЩО ЗРОБЛЕНО:
> • Власник прогнав оновлений gemini_check.py. Результат: `gemini-3.5-flash` → HTTP 503 (НЕ 404!) —
>   тобто назва моделі ВАЛІДНА, резолвиться; 503 = транзієнтне «high demand». `2.5-flash-lite` → 404
>   (теж закрита для нових), `2.0-flash` → 429 (квота), `flash-latest` → 503. Отже вибір 3.5-flash вірний.
> • gemini_check.py: підсумок був занадто грубий (503/429 рахував як провал). Переписано класифікацію:
>   200=працює, 404=закрита(міняти), 503/429=назва валідна(транзієнт/квота). Тепер не вводить в оману.
> • БАГ-008 перенесено у ✅ Виправлені. Ретрай на 503 у bot.py навмисно НЕ додавав — Gemini резервний
>   (останній у ланцюзі), fallback + щогодинний цикл покривають; зайва складність порушила б «мінімум зламів».
> ВЕРИФІКОВАНО: py_compile gemini_check.py OK. Бойові виклики Gemini — власник (діагностика вище).
> КРОКИ ВЛАСНИКА: 1) Закомітити ВСЕ з цієї+минулої під-сесії:
>   `git add bot.py gemini_check.py BUGS.md STATUS.md && git commit -m "БАГ-008: Gemini 2.5-flash→3.5-flash, лог тіла помилки, діаг" && git push`.
>   (Окремий commit-крок з минулого блоку більше не потрібен — цей його заміщує.)
> ⏭ НАСТУПНА СЕСІЯ: БАГ-006 (якість переказу — спостереження на 3.5-flash як резерві), БАГ-007 (Twitter 401).

**Оновлено 2026-07-13 (Z-4 дедуп подій):** власник помітив дубль (та сама подія
з різних джерел). Додано в `bot.py` `is_duplicate_title` — перед публікацією
порівнює заголовок кандидата з опублікованими за 24 год (Жаккар на префіксах слів,
поріг 0.5). Ловить дублі з різних джерел, але НЕ зливає різні події одного міста
(перевірено 6/6: Херсон-АЗС vs Херсон-автівка лишаються окремими). Комітити `bot.py`.
URL-дедуп лишився як був. Схожі заголовки тепер не проходять і не витрачають токени.

**Оновлено 2026-07-13 (БАГ-005 закрито):** `war_stats.py` переписано на стабільний
JSON-API втрат (russianwarship.rip) — тепер постить текст із цифрами й приростом
за добу замість крихкої картинки з сайту ЗСУ (той став суцільним JS). Дедуп раз/добу
збережено. Верифіковано на реальних даних API. Комітити `war_stats.py`, `BUGS.md`.

**Останнє оновлення:** 2026-07-13, 1-а сесія (Cowork, [OPUS]) — старт за схемою:
інструкції проекту, ROADMAP; Z-2 (модель → 70b) + Z-1 (промпт проти помилок) +
Z-3 (спам втрат); Z-2b (ланцюг провайдерів Cerebras→Groq→Gemini). ⚠ Знайдено:
Twitter віддає 401 Unauthorized (див. блок нижче — питання прав/ключів).

---

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, авто-ревізія джерел + курс НБУ — [OPUS]).
> ЗАЧІПАЄ: нове `feed_check.py`, `scheduler.py`, `digest.py`.
> ПЕРЕВІРКА ГІТА: власник усе зробив правильно — коміт 985efb8 видалив
> __pycache__/*.pyc і додав .gitignore; репо чистий. («index corrupt» у пісочниці
> — глюк git через Windows-індекс, репо не стосується.)
>
> ЩО ЗРОБЛЕНО:
> • Авто-ревізія RSS: новий `feed_check.py` — раз на добу парсить усі фіди й шле
>   адміну список мертвих/порожніх (або «усі живі ✅»). Scheduler викликає його
>   раз/добу (daily_log "feed_check", від 6:00). Так ревізія джерел автоматична.
> • Курс НБУ у ранковому дайджесті: `get_rates()` у digest.py (безкоштовний API
>   bank.gov.ua, без ключа) → рядок «💵 Долар … 💶 Євро …». Захищено try/except:
>   якщо API не відповість — дайджест просто без курсу.
>
> ВЕРИФІКОВАНО: feed_check `py_compile` OK; scheduler (хост) — блок feed_check
>   коректний; парсинг курсу на зразку відповіді НБУ — правильний формат.
>   (NBU API через web_fetch віддав порожньо — структура задокументована й стандартна;
>   підтвердимо з реального ранкового дайджесту. Пісочниця й далі ріже великі файли — хост.)
>
> КРОКИ ВЛАСНИКА: закомітити `feed_check.py`, `scheduler.py`, `digest.py`, push.
> Завтра вранці має прийти: дайджест із курсом + звіт feed_check (які RSS мертві).
>
> ⏭ ДАЛІ: БАГ-005 — полагодити `war_stats.py` (розібрати поточну структуру сторінки
> ЗСУ); потім — почистити мертві фіди за звітом feed_check; ідея з Telegram-джерелами.

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, публічний репо + більше України — [OPUS]).
> ЗАЧІПАЄ: `bot.py`, нове `.gitignore`.
>
> КОНТЕКСТ: власник зробив репо ПУБЛІЧНИМ (→ Actions-хвилини необмежені, лишаємо
> `*/15`). Провів АУДИТ БЕЗПЕКИ: у коді й у ВСІЙ історії (947 комітів) — жодних
> секретів, `.env` ніколи не було, пошти/chat_id хардкоду нема, усе через
> os.environ/Secrets. Бази — лише історія новин. РЕПО БЕЗПЕЧНО ПУБЛІЧНИЙ.
>
> ЩО ЗРОБЛЕНО:
> • `.gitignore` додано (__pycache__, .env, venv, IDE). У репо були закомічені
>   __pycache__/*.pyc — власник прибирає: `git rm -r --cached __pycache__` +
>   commit/push.
> • Більше про Україну: додано 2 перевірені воєнні RSS (armyinform.com.ua/feed,
>   militarnyi.com/uk/feed); нова функція `ukraine_score` — новини про Україну
>   сортуються ПЕРШИМИ (лог-тест: воєнне 8/4 бали вище за науку/іноземне 0), але
>   різноманіття лишається. MAX_POSTS_PER_RUN 2→3.
>
> ВЕРИФІКОВАНО: ukraine_score — сорт коректний (Україна першою); нові RSS
> віддають валідний application/rss+xml (перевірено web_fetch). Аудит секретів — чисто.
>
> КРОКИ ВЛАСНИКА: закомітити `bot.py` + `.gitignore`; окремо прибрати сміття:
> `git rm -r --cached __pycache__` → commit → push.
>
> ⏭ ДАЛІ (повна ревізія джерел — почато): решту ~20 наявних RSS перевірити на
> «живість». Найрозумніше — додати у підсумок адміну звіт про мертві фіди (щоб
> аудит був автоматичний, а не ручний). + курс НБУ (Z-7), БАГ-005 (war_stats),
> ідея власника: тягнути з офіційних Telegram-каналів медіа/держустанов (t.me/s).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, «живий режим» + оцінка якості — [OPUS]).
> ЗАЧІПАЄ: `scheduler.py` (переписано), `bot.py`, `.github/workflows/news_bot.yml`.
>
> ВІДКРИТТЯ: кеш `t.me/s` обходиться через `?before=<велике число>` — я бачу
> найсвіжіші пости сам, допомога власника з цим не потрібна.
> ОЦІНКА ЯКОСТІ нових моделей (gemma-4-31b/gpt-oss-120b, пости 3114–3123):
> помітно КРАЩЕ — стисло, без «води», без вигаданих посад/статі; пішла наука/
> здоров'я (фільтр-фікс працює). Дрібниця: калька «кальцифікующего» (підчистимо).
>
> ЩО ЗРОБЛЕНО (Oracle скасовано — картка не варіант; лишаємось на GitHub, безкоштовно):
> • «ЖИВИЙ РЕЖИМ»: cron `0 * * * *` → `*/15 * * * *`; MAX_POSTS_PER_RUN 5→2.
>   Тепер струмочок постів, а не залп раз на годину.
> • `scheduler.py` переписано під часті запуски: новини — щоразу; дайджести й
>   war_stats — РАЗ НА ДОБУ через таблицю `daily_log` (morning від 6:00, evening
>   від 21:00, war_stats від 6:00). Захист від повторів.
> • Воркфлоу: `concurrency` group — без накладання прогонів (щоб не було git-push
>   конфліктів при 96 запусках/добу).
> • notify_admin шле підсумок ЛИШЕ коли є пост або помилка (щоб не спамити 96×/добу).
>
> ВЕРИФІКОВАНО: scheduler.py `py_compile` OK; логіка daily_log — 6/6 на чистій БД
> (morning/evening/war_stats незалежні, повторний mark безпечний). (Копію
> published.db пісочниця дала пошкодженою — mount; тест зроблено на чистій БД.)
>
> КРОКИ ВЛАСНИКА: закомітити `scheduler.py`, `bot.py`, `news_bot.yml`, push.
> Далі канал сам оживе (пости кожні ~15 хв). Я контролюю через t.me/s ?before + підсумки.
> ⏭ ДАЛІ: підчистити дрібні кальки промптом; полагодити war_stats (БАГ-005);
> Z-7 (курс НБУ). Постійний воркер — відкладено (без картки; колись — VPS/домашній пристрій).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, підтвердження здоров'я — [OPUS]).
> ПІДСУМОК ВІД БОТА (перший реальний): «опубліковано 5 з 12; Cerebras×3, Groq×2;
> Gemini 404; Cerebras 429 (один раз, перемкнувся)». ВИСНОВОК: пайплайн ЗДОРОВИЙ,
> самодіагностика працює. Попередній «0 постів» був через відсутність свіжих новин.
> • Gemini 404: ендпоінт/модель правильні (перевірено в доках Google) → причина в
>   ключі/проекті. Переставив Gemini в КІНЕЦЬ черги: Cerebras→Groq→Gemini. Поки два
>   робочі відповідають, Gemini не викликається — 404 зникає. Полагодити ключ згодом.
> • Нюанс видимості: `t.me/s` кешується — найновіші пости я бачу із затримкою;
>   найшвидша правда — підсумок від бота.
> КРОК ВЛАСНИКА: закомітити `bot.py`, push. Оцінку якості нових моделей
> (gemma-4-31b/gpt-oss-120b) зроблю з наступних постів, коли оновиться кеш.
> ⏭ ДАЛІ: старт міграції на постійний воркер (Oracle Free Tier) — власнику
> створити безкоштовну VM; я готую worker.py (черга + APScheduler + локальна БД).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, самодіагностика — [OPUS]) — прибираємо «сліпоту».
> ЗАЧІПАЄ: `bot.py`, `.github/workflows/news_bot.yml`.
> ПРИЧИНА: після ревізії моделей у веб-версії каналу нових постів не з'явилось —
> або не було свіжих новин, або всі моделі відпали. З обрізаних логів не видно.
>
> ЩО ЗРОБЛЕНО:
> • `notify_admin()` у bot.py: наприкінці кожного запуску шле адміну (через
>   FEEDBACK_BOT_TOKEN/ADMIN_CHAT_ID) короткий підсумок — скільки опубліковано з
>   N кандидатів, які моделі спрацювали (Gemini×2…), які помилки (Cerebras 404…).
> • `call_llm` веде STATS (успіхи/остання помилка на провайдера).
> • Workflow: у крок scheduler додано FEEDBACK_BOT_TOKEN + ADMIN_CHAT_ID.
> ЗАВДЯКИ ЦЬОМУ: замість повних логів власник пересилає 1 рядок — видно стан.
>
> ВЕРИФІКОВАНО: логіка підсумку — 2 сценарії (успіх + повний провал) дають
> правильний текст; блок notify_admin/STATS на хості коректний. (Пісочниця й далі
> б'є mount-артефактом — хост джерело правди.)
>
> КРОКИ ВЛАСНИКА: закомітити `bot.py` + `news_bot.yml`; push; Run workflow →
> у Telegram від фідбек-бота прийде підсумок — перешли мені той рядок.
> (Секрети FEEDBACK_BOT_TOKEN/ADMIN_CHAT_ID уже існують — фідбек-бот ними працює.)
>
> НАПРЯМ (обрано мною за дорученням власника): ідемо до постійного воркера
> (Oracle Free Tier) як фундаменту живого каналу + тривог. Послідовність:
> A) самодіагностика ✅ → B) підтвердити, що пайплайн публікує → C) черга-крапельниця
> (переноситься у воркер) → D) міграція на Oracle Free Tier (постійний процес,
> стан у локальній БД, без git-коммітів). D потребує дії власника (створити VM) —
> підготую покроково.

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, ревізія моделей + якість — [OPUS]) — велике.
> ЗАЧІПАЄ: `bot.py`, `digest.py`; нове `BUGS.md`.
>
> КЛЮЧОВЕ ВІДКРИТТЯ 1: я можу САМ читати канал через `https://t.me/s/uanewsbyai`
> (п