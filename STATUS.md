# UA News — Стан проекту (STATUS.md)

> Читається на початку кожної сесії. Оновлюється в кінці. Не видаляти!

> ➕ ДОПОВНЕННЯ ДО СЕСІЇ 18 (2026-08-08) — username каналу більше не може
> зупинити публікацію + підготовка до зміни публічного посилання.
> ЗАЧІПАЄ: digest.py, .github/workflows/news_bot.yml, README.md, ROADMAP.md.
> НАСЛІДКИ ЗАПУСКУ: новий НЕОБОВ'ЯЗКОВИЙ secret TELEGRAM_CHANNEL_USERNAME;
> cron не змінювався. Без цього secret поведінка 1-в-1 як була.
>
> ⚠️ ЧОМУ ЦЕ ЗРОБЛЕНО РАНІШЕ, НІЖ ЗМІНЕНО USERNAME: TELEGRAM_CHANNEL_ID у
> секретах зберігався у формі «@ім'я» (доведено не здогадом: вечірній
> дайджест #9958 містить робочі посилання «→ пост», а digest.py будував їх
> ЛИШЕ коли CHANNEL_ID починається з «@»). Тобто зміна публічного посилання
> в Telegram поклала б канал повністю: наступний прогін стукав би в
> неіснуючий @uanewsbyai і отримував «chat not found». Це не гіпотетичний
> ризик — це був наступний крок у плані Етапу 0.
> ЩО ЗРОБЛЕНО: у digest.py з'явилась чиста функція resolve_username(
> channel_id, explicit) і змінна CHANNEL_USERNAME із нового secret.
> Пріоритет: явний TELEGRAM_CHANNEL_USERNAME → «@ім'я» з CHANNEL_ID → None
> (тоді дайджест просто не додає посилань, як і раніше). Це дозволяє
> перевести TELEGRAM_CHANNEL_ID на ЧИСЛОВИЙ id (-100…), який не змінюється
> ніколи, і надалі міняти публічне посилання без ризику для публікації.
> Інші файли чіпати не довелося: у жодному .py немає зашитого username —
> bot.py:77, digest.py:9, war_stats.py:10 читають лише оточення.
> ПОПУТНО: у README.md виправлено 3 мертві посилання t.me/ua_news_channel
> (такого каналу немає й не було) → після зміни username вони ведуть на
> t.me/novyny_ua_svit. Це був хибний лінк у публічній документації незалежно
> від зміни username. GROWTH.md (рядки 21, 57, 82 і розділ 4.2) теж оновлено.
> ВЕРИФІКОВАНО (бойові API не смикались): py_compile digest.py ✅;
> news_bot.yml — валідний YAML ✅; resolve_username прогнано на 9 кейсах —
> стара поведінка «@нік без нового secret» дає той самий результат, явна
> змінна перекриває CHANNEL_ID, числовий id без змінної не ламається,
> пробіли й «@» самі по собі оброблені ✅; CHANNEL_USERNAME при незаданому
> secret = "" → поведінка стара ✅.
>
> 🔎 USERNAME — ПЕРЕВІРЕНО ЗАЙНЯТІСТЬ (метод: сторінка t.me/<ім'я>; зайняте
> дає заголовок «Telegram: View @…» з назвою й кількістю підписників, вільне —
> «Telegram: Contact @…» з порожньою метою; метод звірено на @novyny_ukrayiny,
> який власник назвав зайнятим — і сторінка справді показала чужий канал
> «Западняк | Новини Львова», 21 771 підписник).
> ВІЛЬНІ на 08.08: @novyny_ua_svit · @ua_novyny_svitu · @novyny_ukrainy_ta_svitu.
> ⚠️ Перевірка непряма — фінальне слово за самим Telegram у налаштуваннях.
>
> 📌 ВАЖЛИВО ПРО ВАРТІСТЬ ЗМІНИ (поправка до GROWTH.md 4.2): зміна публічного
> посилання в Telegram БЕЗКОШТОВНА і необмежена; Fragment — окремий ринок
> колекційних імен, до цього стосунку не має. Дорожчають не гроші, а зламані
> посилання. Плюс те, чого в GROWTH.md немає: старий @uanewsbyai звільняється
> МИТТЄВО і його може зайняти будь-хто, зокрема для видавання себе за нас —
> при 5 підписниках дешево, при 5000 це готовий інструмент шахрая.
> І ще поправка: пошуковий ефект від username менший, ніж записано —
> username лише латиницею, а українець шукає кирилицею, тож пошуковий важіль
> уже смикнуто зміною НАЗВИ. Username варто міняти заради довіри й
> запам'ятовуваності, а не заради пошуку.
>
> ✅ НАЗВУ КАНАЛУ ЗМІНЕНО ВЛАСНИКОМ на «Новини України та світу | UA News» —
> Етап 0 GROWTH.md: назва ✅, опис ✅, username ❌ (лишається).
>
> ✅ USERNAME ЗМІНЕНО ВЛАСНИКОМ 08.08 на **@novyny_ua_svit** — Етап 0 GROWTH.md
> закрито повністю (назва ✅, опис ✅, username ✅).
> ⚠️ ПОРЯДОК ДІЙ ПІШОВ НЕ ЗА ПЛАНОМ, АЛЕ КАНАЛ ВЦІЛІВ: власник змінив username
> ДО переходу на числовий id, і одночасно перевів TELEGRAM_CHANNEL_ID на
> «@novyny_ua_svit». Саме це й урятувало — якби secret лишився «@uanewsbyai»,
> кожен прогін падав би з «chat not found». ПЕРЕВІРЕНО В БОЮ: пости 10181–10184
> о 11:01 і 11:06 UTC (уже після зміни) вийшли в канал нормально, ланцюг
> комітів «update db» не рвався. Старий @uanewsbyai перевірено — ВІЛЬНИЙ,
> тобто його може зайняти будь-хто.
> ⚠️ АЛЕ ГОЛОВНЕ НЕ ЗРОБЛЕНО: TELEGRAM_CHANNEL_ID досі у формі «@ім'я», а не
> числовий. Тобто захист, заради якого писалась resolve_username(), НЕ
> увімкнено — наступна зміна публічного посилання так само покладе канал.
>
> КРОКИ ВЛАСНИКА (те, що лишилось):
>   1) `git add -A && git commit -m "Username каналу відв'язано від адреси
>      публікації; посилання оновлено на @novyny_ua_svit"` → pull --rebase →
>      push. ⚠️ ЦЕ ЩЕ НЕ ЗРОБЛЕНО: правки digest.py і news_bot.yml лежать
>      лише локально, у бою їх немає — TELEGRAM_CHANNEL_USERNAME зараз навіть
>      не передається в digest.py. Зараз це нічого не ламає (CHANNEL_ID у
>      формі «@ім'я», посилання будуються старим шляхом), але й не захищає.
>   2) Дізнатися ЧИСЛОВИЙ id каналу: відкрити в браузері
>      `https://api.telegram.org/bot<ТОКЕН>/getChat?chat_id=@novyny_ua_svit`,
>      узяти поле "id" (виглядає як -1001234567890). Токен нікуди не
>      вставляти, крім адресного рядка, і посилання нікому не показувати.
>   3) У GitHub → Settings → Secrets → Actions додати
>      TELEGRAM_CHANNEL_USERNAME = novyny_ua_svit і ЗМІНИТИ
>      TELEGRAM_CHANNEL_ID на числовий id із кроку 2. Порядок: спершу додати
>      USERNAME, потім міняти ID — інакше вечірній дайджест лишиться без
>      посилань «→ пост».
>   4) Дочекатись прогону (вони о :01/:16/:31/:46) і перевірити, що пост
>      вийшов. Посилання «→ пост» перевіряються ввечері, у дайджесті 21:00.
>   5) Оновити опис проекту на GitHub і біо у Twitter на нове посилання
>      (README.md і GROWTH.md уже оновлено в цій сесії).
>   6) Досі не зроблено: `python feed_check.py` — блокує issue #7.
>   7) Завести issue на БАГ-014 (текст готовий у чаті сесії).
> ⏭ НАСТУПНА СЕСІЯ: issue #7 (published.source + звіт по джерелах), разом із
>   ним Z-12 / БАГ-014 — обидва про те, ЯК відбираються новини.

> ✅ ЦЯ СЕСІЯ (2026-08-08, 18-а — [OPUS]) — ревізія роботи каналу за добу
> (вікно 07.08 13:25 → 08.08 13:25 Київ = 10:25 → 10:25 UTC). Змін у коді НЕМАЄ,
> тільки аудит + новий баг у журнал.
> ЗАЧІПАЄ: STATUS.md, BUGS.md. НАСЛІДКИ ЗАПУСКУ: нічого.
>
> ПЕРШЕ, ЩО ЗВІРЕНО (урок сесії 15): git hash-object по STATUS.md і BUGS.md
> у локальній папці й на origin збігся — правки лягають на актуальну версію.
> ⚠️ Локальна гілка при цьому ВІДСТАЄ на 35 комітів «update db» — аналіз
> робився НЕ по локальній published.db (вона застигла на 07.08 08:32), а по
> свіжій копії з origin. Локальна БД для аудиту непридатна без `git pull`.
>
> ПАЙПЛАЙН — ЗДОРОВИЙ, НАЙКРАЩА ДОБА ЗА ВЕСЬ ЧАС ЗА ВТРАТАМИ:
> • 317 постів реально в каналі (msg_id 9851→10170). Попередні доби: 326, 257,
>   314, 319. Пропущених msg_id у діапазоні лише 3 (9958, 10076, 10077) — це
>   вечірній дайджест 07.08, ранковий 08.08 і втрати ЗСУ, тобто службові пости.
> • 🏆 Лише 9 новин «з'їдено» злитими групами (published без msg_id).
>   Динаміка: 30.07 — 64, 01.08 — 88, 04.08 — 38 (split_unrelated_groups),
>   05.08 — 10, 06.08 — 17, 07.08 — 7. Фікс сесії 14 тримається; залишок
>   БАГ-012 (issue #6) уже не масовий, а одиничний.
> • Простоїв немає: коміти БД ідуть суцільно, найбільша пауза між постами —
>   16,4 хв, між комітами — 17,6 хв. Збій GitHub Actions 06.08 не повторився.
> • Службові запуски на місці: ранковий і вечірній дайджест 07.08, ранковий
>   08.08, war_stats 07 і 08.08 (за 06 і 07.08), feed_check обидві доби.
> • ✅ БАГ-013 ПІДТВЕРДЖЕНО В БОЮ (обіцяна перевірка сесії 17): за добу після
>   фіксу — 0 нових posted_title без літер; за весь час у базі так само 5
>   старих випадків, нових не додалось.
> • ✅ Міграція приватності фідбеку пройшла: у бойовій БД схема таблиці
>   feedback = update_id / user_hash / text / received_at, особистих колонок
>   немає. Рядків 0 — писати нікому.
> • Дедуп працює: серед 317 постів жодної пари з лексичною схожістю ≥0,5 у
>   вікні 12 годин. Відсіяно 272 кандидати: duplicate_event 169 (62%),
>   duplicate 74 (27%), ai_skip 16 (6%), opinion 11 (4%), bad_output 2.
>   Тобто ~46% усього, що дійшло до курації, — це повтори події з іншого
>   джерела, і вони гасяться до публікації.
> • Twitter: у twitter.db 0 рядків — це НЕ збій, крос-постинг свідомо
>   заморожено в news_bot.yml (`if: false`, БАГ-007 / issue #2).
>
> 🐞 НОВЕ — БАГ-014: «трендовість» насправді вимірює ЗАСТАРІЛІСТЬ (деталі в
> BUGS.md). Коротко: update_topic_count() у fetch_news викликається для КОЖНОГО
> кандидата на КОЖНОМУ прогоні, тому seen_topics.count = скільки прогонів
> новина провисіла у стрічці, а не скільки джерел про неї написали. Доказ:
> топ-20 ключів за добу — англомовний мотлох із tech-стрічок (trough 78,
> ‘rubberz’ 71, wacom’s 65, movinkpad 65) при стелі 96 прогонів на добу;
> медіана по 1065 нових ключах — 2. Оскільки get_topic_count — другий ключ
> сортування, він піднімає найлежалішу новину, а не найгарячішу. Це прямо
> суперечить обіцянці каналу «в реальному часі». Ефект обмежений тим, що
> перший ключ (ukraine_score) домінує, але в англомовному кошику (en[:3])
> ukraine_score майже завжди 0 — там вирішує саме цей зіпсований лічильник.
> Побічно: seen_topics росте без прибирання — 71 460 рядків, +1065 за добу.
>
> 📊 ПРОДУКТ — ГОЛОВНА ПРОБЛЕМА НЕ ЗРУШИЛА: t.me/uanewsbyai на 08.08 —
> **5 підписників**. НАЗВУ вже змінено на «Новини України та світу» ✅ (Етап 0
> GROWTH.md частково виконано), опис оновлено. USERNAME досі @uanewsbyai ❌ —
> при 5 підписниках зміна ще безкоштовна. За добу 317 постів (один кожні
> 4,5 хвилини) для 5 людей.
>
> ⚠️ ДРІБНЕ СПОСТЕРЕЖЕННЯ (без дії): за добу 120 комітів «update db» при
> cron */15, який дає щонайбільше 96 слотів. Основна сітка чиста (:01, :16,
> :31, :46), зверху ~1 позачерговий коміт на годину. На канал не впливає;
> звірити при нагоді, чи це черга concurrency, чи зайві прогони.
>
> ВЕРИФІКОВАНО (бойові API не смикались; бойова БД лише читалась, копія з
> origin): усі числа — SQL по published/skipped/seen_topics/daily_log свіжої
> published.db з origin (d9749a7); кадеція — по датах комітів; гіпотеза
> БАГ-014 перевірена по коду bot.py (рядки 678–697) і зіставленням топ-ключів
> зі списком опублікованих і відсіяних заголовків; підписники/назва — з
> публічної сторінки t.me/uanewsbyai.
>
> КРОКИ ВЛАСНИКА: 1) `git pull --rebase` у локальній папці — вона відстає на
>   35 комітів. 2) Етап 0 GROWTH.md, залишок: USERNAME (назву вже зроблено) —
>   5 хвилин, поки безкоштовно. 3) Завести issue на БАГ-014. 4) Досі не
>   зробленe з минулої сесії: `python feed_check.py` і сказати, які фіди
>   мертві — це розблокує issue #7.
> ⏭ НАСТУПНА СЕСІЯ: issue #7 (published.source + звіт по джерелах) — далі
>   аудит джерел неможливо робити SQL-запитом, доводиться парсити Telegram.
>   Разом із ним природно лягає БАГ-014: обидва про те, ЯК відбираються
>   новини. Далі issue #6 (семантичний чек груп курації).

> ✅ ЦЯ СЕСІЯ (2026-08-07, 17-а — [OPUS]) — приватність фідбеку (виправлено)
> + ревізія за добу + issues #5/#6/#7.
> ЗАЧІПАЄ: feedback_bot.py (user_pseudonym, нова схема таблиці feedback +
> одноразова міграція з VACUUM); bot.py (headline у main() — БАГ-013).
> НАСЛІДКИ ЗАПУСКУ: нічого — без нових secret, без зміни cron; міграція
> автоматична й ідемпотентна.
>
> ПЕРШЕ, ЩО ЗВІРЕНО (урок сесії 15): origin і локальна гілка збігались на
> d079093 перед початком роботи.
>
> 🔒 ГОЛОВНЕ ЗА СЕСІЮ — ДІРА В ПРИВАТНОСТІ, ЗНАЙДЕНА ДО ІНЦИДЕНТУ:
> published.db комітиться в ПУБЛІЧНИЙ репозиторій (це навмисно — публічність
> дає безлімітні хвилини Actions, на них тримається весь безкоштовний режим
> проекту). Таблиця feedback при цьому зберігала user_id, @username та ім'я
> підписника. Ланцюг замикався у воркфлоу: news_bot.yml:58 запускає
> feedback_bot.py → news_bot.yml:76 `git add published.db` → push.
> Тобто ПЕРША Ж людина, яка написала б у фідбек-бот, отримала б свою
> ідентичність у git назавжди (видалення рядка історію не чистить).
> Не витекло нічого лише тому, що таблиця порожня — рятує те, на що ми
> скаржимось: 5 підписників і писати нікому.
> ЩО ЗРОБЛЕНО: у базу тепер іде лише незворотний `user_hash` =
> sha256(FEEDBACK_BOT_TOKEN + user_id)[:16]. Сіль — саме токен із секретів,
> бо БЕЗ СОЛІ хеш Telegram-id не захищає нічого: простір id — десяткові
> числа, перебираються за хвилини. Нового secret заводити не треба.
> Ідентичність адміну НЕ втрачена: forward_to_admin як пересилав @username
> та ім'я в Telegram, так і пересилає — цей канал у git не потрапляє.
> Міграція одноразова: текст і час рядків переносяться, id → псевдонім,
> стара таблиця зникає. ALTER TABLE тут не годився — він лишив би старі
> стовпці на місці.
> ⚠️ УРОК, ЯКИЙ ЛЕГКО ПРОҐАВИТИ: перша версія фіксу пройшла всі перевірки
> схеми й ВСЕ ОДНО лишала дані у файлі — SQLite після DROP TABLE звільняє
> сторінки, але не витирає байти, і `vasyl`/`olena` читались у файлі
> шістнадцятковим редактором. Спіймала це лише байтова перевірка сирого
> файлу. Звідси VACUUM у гілці міграції. Для БД, яку комітять у публічний
> репозиторій, «видалив рядок» ≠ «видалив дані».
> ЛИШАЄТЬСЯ СВІДОМО: сам ТЕКСТ повідомлення зберігається — він і є сигнал,
> заради якого таблиця існує. Якщо підписник напише туди телефон, він піде
> в публічний репозиторій. Кодом це не лікується, лише руками.
> ВЕРИФІКОВАНО (бойові API не смикались, бойова БД лише читалась):
> py_compile ✅; на КОПІЇ бойової published.db — особисті колонки зникають,
> published/skipped не зачеплені, повторний init ідемпотентний ✅; на
> синтетичній старій схемі з 3 рядками — тексти й час не втрачені, один
> user_id дає стабільний псевдонім, різні id — різні ✅; у СИРИХ БАЙТАХ
> файлу слідів username немає ✅; інший токен → інший псевдонім ✅.
>
> 🔴 ЗБІЙ GITHUB ACTIONS (не наш): глобальний інцидент GitHub, Actions +
> Pages, impact Critical, почався 15:22 UTC 06.08. Наші прогони падали з
> 15:45 до 19:15 UTC (`Failed to resolve action download info`, далі
> `The job was not acquired by Runner of type hosted` — джоб навіть не
> стартував), плюс два афтершоки о 20:30 і 20:45. Канал мовчав ~3,5 години.
> Коду це не стосується, правити нема чого; concurrency-група `news-bot` +
> `ref: main` спрацювали як задумано — черга виконалась послідовно, конфлікту
> на published.db при відновленні НЕ було.
> ЦІНА ПРОСТОЮ: ~37 постів. За вікно 16:00–19:00 UTC вийшло 11 постів проти
> звичайних ~48. Вони втрачені НАЗАВЖДИ: fetch_news бере з кожного фіда лише
> entries[:5], тож усе, що з'їхало нижче п'ятої позиції, наступний прогін уже
> не бачить. Аргумент на користь більшого вікна entries, якщо простої
> повторюватимуться.
>
> РЕВІЗІЯ ЗА ДОБУ (вікно 06.08 10:51 → 07.08 10:51 Київ, копія бойової БД):
> • 247 постів у канал (доба до цього 304, ще раніше 281). Просідання — рівно
>   вартість простою, не регрес пайплайну.
> • Порожніх годин 3, усі три — простій GitHub. Решта доби рівна: 11–15
>   постів на годину, вночі 6–14 (тихий режим глушить звук, не публікацію).
> • 🟢 ДЕДУП ТРИМАЄ: 0 пар із Жаккаром ≥ 0.6 по posted_title серед 247
>   реально опублікованих. Відсіяно 169: duplicate_event 107, duplicate 44,
>   opinion 14, ai_skip 4. Брак від моделі (bad_output) — 0 разів.
> • ✅ ЕФЕКТ split_unrelated_groups ТРИМАЄТЬСЯ І ПОГЛИБЛЮЄТЬСЯ. Новин, що
>   гинули у злитих групах: 31.07–03.08 — 58–88 на добу; 04.08 — 38;
>   05.08 — 10; 06.08 — 17. Це найстійкіше покращення проекту.
> • Заголовки: n=247, min 1, середній 72, max 117, довших за 160 — жодного.
> • Розклад цілий: дайджести ранок+вечір 06.08, ранок 07.08; war_stats і
>   feed_check щодня, включно з добою простою.
>
> ✅ БАГ-013 ВИПРАВЛЕНО ЦІЄЮ Ж СЕСІЄЮ (issue #5 закрито комітом): заголовком
> стає перший рядок, де є хоча б 3 літери; якщо такого немає — None, а не
> відкат на перший непорожній (NULL безпечний завдяки COALESCE на
> RSS-заголовок, «🚨» у базі — ні).
> ЧАСТОТА ВИЯВИЛАСЬ ВИЩОЮ ЗА ОЦІНКУ: не «3 за 3 тижні», а 5 випадків за весь
> час (msg 5279, 5516, 7598, 9065, 9613) — ~раз на 4 дні.
> 🔍 І МЕХАНІЗМ ШКОДИ БУВ ЗАПИСАНИЙ НЕТОЧНО — перевірка по коду це виправила:
> лексичний is_duplicate_title вбити новину НЕ міг (`_title_words("🚨")` дає
> порожню множину, такий рядок просто пропускається). Міг семантичний:
> is_semantic_duplicate бере COALESCE(posted_title, title) і показує «🚨»
> моделі — кандидат «🚨» проти збереженого дав би ДУБЛЬ і поклав би всю злиту
> групу. Не спрацювало лише тому, що з 5 випадків жодні два не потрапили в
> одне 24-годинне вікно (з них 3 збіги між собою — тобто ризик був справжній).
> ЗАТЕ ЩОРАЗУ ставалось те, чого гіпотеза не називала: такий пост на добу
> лишається в базі дедупу БЕЗ заголовка, і справжні дублі саме цієї події не
> ловить жоден із двох шарів. Дірка була не потенційна, а вже реалізована.
> УРОК: перш ніж лагодити за записаною гіпотезою, варто звірити її з кодом —
> півгіпотези виявилось хибною, і виправлення стало точнішим.
>
> ПРОДУКТ — БЕЗ ЗМІН, І ЦЕ ДРУГИЙ РІК ПОСПІЛЬ ГОЛОВНА ПРОБЛЕМА:
> t.me/uanewsbyai на 07.08 — **5 підписників**. Опис каналу оновлено (містить
> «новини України та світу», «Довіряй перевіреному») — це частина Етапу 0.
> Але НАЗВА досі «UA News», username досі @uanewsbyai — тобто фактор №1 у
> пошуку Telegram не зачеплений. Українець, який шукає «новини України»
> кирилицею, канал не знайде. За добу бот написав 247 постів для 5 людей.
>
> КРОКИ ВЛАСНИКА: 1) Етап 0 з GROWTH.md — НАЗВА і USERNAME (5 хвилин;
>   при 5 підписниках зміна username безкоштовна, потім дорожчає назавжди).
>   2) `python feed_check.py` і сказати, які фіди мертві — це розблокує
>   issue #7. 3) Після найближчого прогону перевірити, що схема feedback
>   мігрувала (колонки update_id/user_hash/text/received_at).
> ⏭ НАСТУПНА СЕСІЯ: issue #7 (published.source + звіт по джерелах) — блокує
>   рішення про квоту на нішеві теми, і для нього потрібен feed_check від
>   власника. Далі issue #6 (семантичний чек груп курації). Задача
>   «✅ Підтверджено N джерелами» (GROWTH.md 5.1) лишається єдиною в плані,
>   що дає читачеві причину підписатися.
> ПЕРЕВІРИТИ В БОЮ (наступна сесія): за добу після 07.08 не має з'явитися
>   жодного нового posted_title без літер — запит є в BUGS.md, розділ БАГ-013.

> ➕ ДОПОВНЕННЯ ДО СЕСІЇ 16 (2026-08-06) — аудит ДЖЕРЕЛ: чи всі 28 фідів
> реально доходять до каналу. Змін у коді немає.
>
> МЕТОД: список джерел узято з RSS_FEEDS у bot.py (28 фідів). Оскільки в
> published.db НЕМАЄ колонки джерела, фактичний розподіл довелося знімати
> з самого каналу: розібрано підпис «📰 За даними: …» у 160 останніх постів
> (msg 9502–9661, ≈13 годин). Живість фідів із цієї пісочниці перевірити
> не вдалося — вихід у зовнішні сайти тут закритий; це обмеження
> середовища, а не каналу (перевірка — feed_check.py у власника).
>
> РОЗПОДІЛ ЗА 160 ПОСТІВ (основне джерело поста):
> Цензор.НЕТ 26 (16%), Суспільне 20 (12%), NV 18 (11%), АрміяInform 15 (9%),
> Укрінформ 14 (9%), Reuters 13 (8%), ТСН 12 (8%), УНІАН 8 (5%), LB.ua 8 (5%),
> Українська правда 6 (4%) — разом 89%. Далі поодинокі: Генштаб ЗСУ 2,
> BBC Україна 2, BBC 2, Європейська правда 2, slovoidilo 2, AP / Al Jazeera /
> Euronews / Факти ICTV / 24 Канал / Бабель / Вечірній Київ / 0352.ua /
> Відомо / Український південь — по 1.
>
> 🔴 12 З 28 ДЖЕРЕЛ ЗА 13 ГОДИН НЕ ДАЛИ ЖОДНОГО ПОСТА:
> Радіо Свобода, DW, Бігус.Інфо, Мілітарний, The Guardian, CNN, DOU,
> TechCrunch, The Verge, Ars Technica, ScienceDaily (+ «Google News» як
> підпис — і це нормально: бренд підмінюється справжнім виданням із тегу
> <source>, саме звідти в каналі беруться Бабель, 24 Канал, Факти ICTV,
> 0352.ua тощо — тобто Google News працює як розширювач переліку джерел).
>
> ДВІ МОЖЛИВІ ПРИЧИНИ, ЇХ ТРЕБА РОЗДІЛИТИ:
> 1) фід мертвий (CNN на http rss.cnn.com — найпідозріліший; gnews-запити
>    when:1d для Радіо Свободи й DW могли перестати віддавати записи);
> 2) системне голодування у відборі. У fetch_news: з кожного фіда беруться
>    лише entries[:5], далі сортування за ukraine_score, далі
>    picked = ua[:9] + en[:3], далі MAX_POSTS_PER_RUN = 3. Тобто за прогін у
>    канал виходять ~3 теми з ~12 кандидатів. Джерело з низьким ukraine_score
>    (DOU, TechCrunch, The Verge, Ars Technica, ScienceDaily — усі lang=en і
>    конкурують за 3 англомовні слоти з Reuters/AP/BBC про війну) не виграє
>    майже НІКОЛИ. Це не баг, це наслідок ваг — але наслідок незапланований.
> Англомовні загалом проходять (9–26% постів на добу), тобто en-квота працює;
> провалюється саме НЕвоєнна тематика.
>
> ⚠️ АРХІТЕКТУРНА ДІРА, ЯКУ ЦЕ ОГОЛИЛО: published.db не зберігає джерело
> поста. Питання «чи всі джерела доходять до каналу» неможливо поставити до
> бази — довелося парсити публічну сторінку Telegram. Це має бути один SQL.
>
> 📌 РІШЕННЯ ВЛАСНИКА (06.08): обсяг постів НЕ зменшуємо — більше постів
> означає більше різної інформації. Задачу «зменшити обсяг» знято.
> ДАНІ ДО ЦЬОГО РІШЕННЯ (щоб наступні сесії не сперечались наосліп):
> різноманітність від обсягу зараз НЕ зростає — 89% постів дають 10 видань,
> лише 8% постів мають 2+ джерела, а 12 джерел мовчать. Тобто важіль
> різноманітності — не кількість постів, а ВІДБІР. Тому наступна задача
> працює саме на різноманітність, а не на скорочення.
>
> ⏭ НАСТУПНА СЕСІЯ (обрано за дорученням власника) — «звідки новини»:
> 1) додати колонку published.source (ALTER TABLE, як свого часу posted_title:
>    адитивно, стара база читається без міграції) і писати в неї назву
>    джерела — для злитих груп основне джерело + решту через кому;
> 2) маленький звіт (окрема функція або запит у STATUS) «джерела за 7 діб»,
>    щоб ця ревізія робилася одним запитом, а не парсингом Telegram;
> 3) після 1–2 діб даних — рішення про м'яку квоту на нішеві теми
>    (техніка/наука), щоб DOU/TechCrunch/Verge/Ars/ScienceDaily перестали
>    голодувати. Квоту НЕ додаємо наосліп до появи даних.
> Паралельно дрібниця: БАГ-013 (заголовок «🚨») — 5 рядків у main().
> КРОКИ ВЛАСНИКА ДО НАСТУПНОЇ СЕСІЇ: запустити `python feed_check.py`
> (або подивитись сьогоднішній звіт «🩺 Перевірка RSS» у боті зворотного
> зв'язку) і сказати, які фіди він назвав мертвими — це розділить причину
> «мертвий фід» і «голодування у відборі».

> ✅ ЦЯ СЕСІЯ (2026-08-06, 16-а — [OPUS]) — ревізія роботи каналу за добу
> (вікно 05.08 17:55 → 06.08 17:55 Київ). Змін у коді НЕМАЄ — тільки аудит.
> ЗАЧІПАЄ: STATUS.md, BUGS.md. НАСЛІДКИ ЗАПУСКУ: нічого.
>
> ПЕРШЕ, ЩО ЗВІРЕНО (урок сесії 15): origin і локальна папка збігаються —
> git hash-object по bot.py / GROWTH.md / ROADMAP.md / STATUS.md / scheduler.py
> дав однакові хеші. Локальний коміт 2873ac9 потрапив на origin після rebase
> під іншим хешем. Робота сесії 15 у бою.
>
> ПАЙПЛАЙН — ЗДОРОВИЙ:
> • 301 пост у канал за добу (попередня доба 300, до неї 267). Прогони кожні
>   ~15 хв, ланцюг комітів «update db» безперервний, пропусків прогонів немає;
>   дві паузи 28–29 хв уночі — мовчання стрічок, не збій.
> • Дайджести на місці: ранок+вечір 05.08, ранок 06.08; war_stats і feed_check
>   щодня. Таблиця feedback досі порожня (0 рядків) — писати нікому.
> • ✅ ГОЛОВНИЙ ВИМІРЯНИЙ ЕФЕКТ: split_unrelated_groups (сесія 14, у бою з
>   04.08) вбив залишок БАГ-012 на ~85%. Новин, які гинули у злитих групах
>   (published без msg_id): 28.07–03.08 — 58–93 на добу; 04.08 — 38;
>   05.08 — 10; 06.08 — 16. Це найбільше покращення якості за останні сесії.
> • is_bad_output спрацював 2 рази за добу — брак від моделі в канал не вийшов.
> • Дедуп тримає: за 7 діб серед реально опублікованих 0 повторів заголовка
>   (єдиний виняток — артефакт «🚨», див. БАГ-013 нижче).
> • Заголовки: n=301, min 1, середній 72, max 120 символів, >160 — жодного.
>   Стіна тексту з сесії 14 більше не повторюється.
> • Відсіяно за добу 205: duplicate_event 122, duplicate 55, opinion 21,
>   ai_skip 5, bad_output 2.
>
> ПРОДУКТ — БЕЗ ЗМІН, І ЦЕ ГОЛОВНА ПРОБЛЕМА:
> • t.me/uanewsbyai станом на 06.08: **5 підписників, 3 перегляди на пост**.
>   Назва «UA News», username @uanewsbyai — БЕЗ українських ключових слів.
>   Опис уже містить «новини України та світу» (частина Етапу 0 зроблена),
>   але фактор №1 у пошуку Telegram — саме назва й username, і вони старі.
> • За дві доби після знахідки сесії 15 бот написав ще ~600 постів для 5 людей.
>   Етап 0 з GROWTH.md (15 хвилин руками) досі не виконаний — це єдина
>   безкоштовна дія з великим важелем, і вона дешевшає тільки зараз.
>
> ⚠ ПЕРЕГЛЯД РІШЕННЯ СЕСІЇ 15 (обсяг постів): сесія 15 зняла задачу
> «зменшити обсяг» як неактуальну при 5 підписниках. Ревізія показує, що це
> помилка порядку дій. 301 пост/добу = 12–13 постів щогодини, рівно, без спаду
> вночі (23:00–07:00 Київ теж 12–15 на годину, просто без звуку). Зрілі
> новинні канали дають 30–80 постів/добу; 300 — це стрічка, яку неможливо
> читати. Тобто обсяг не «оптимізація на потім», а ПЕРЕДУМОВА дистрибуції:
> зараз він нікому не шкодить, але кожен підписник, приведений Етапом 1–2,
> прийде на канал, який його завалить і втратить. Правильний порядок:
> спершу привести обсяг до читабельного, потім вести людей.
>
> ВЕРИФІКОВАНО: свіжий клон origin (не локальна копія); аналіз бойової
> published.db лише на читання; ланцюг комітів за 24 год; публічні сторінки
> t.me/uanewsbyai і t.me/s/uanewsbyai. Бойові API не смикались, код не змінено.
> КРОКИ ВЛАСНИКА: 1) Етап 0 з GROWTH.md — назва каналу з українськими
>   ключовими словами + username (5 хвилин, безкоштовно, при 5 підписниках
>   безболісно). 2) git add STATUS.md BUGS.md && git commit -m "Ревізія за добу
>   06.08: пайплайн здоровий, обсяг постів — блокер зростання" && git pull
>   --rebase && git push. 3) Створити issue на БАГ-013 (заголовок «🚨»).
> ⏭ НАСТУПНА СЕСІЯ: рішення власника про порядок — (А) обсяг постів
>   (пріоритет: без цього дистрибуція ллє воду в дірявий кошик) або
>   (Б) «✅ Підтверджено N джерелами» з GROWTH.md 5.1. Плюс дрібний БАГ-013.

> 🔴 ЦЯ СЕСІЯ (2026-08-04, 15-а, частина 2 — [OPUS]) — ГОЛОВНА ЗНАХІДКА
> ПРОЕКТУ: у каналу 5 підписників. Вузьке місце — не бот, а дистрибуція.
> ЗАЧІПАЄ: нічого в коді. Це стратегічна знахідка, зафіксована в стані проекту.
>
> ЯК ВИЯВЛЕНО: перед тим як робити задачу «обсяг постів», перевірив, чи вона ще
> актуальна — відкрив публічну сторінку каналу t.me/uanewsbyai і t.me/s/uanewsbyai.
> ФАКТИ (04.08.2026): 5 підписників; ~3 перегляди на пост; канал працює з
> 16.07.2026 (19 діб), за цей час опубліковано 5218 постів у канал.
> Тобто 5218 постів написано для 5 людей.
>
> ЩО ЦЕ ЗМІНЮЄ: задачу «зменшити обсяг постів» ЗНЯТО з черги як неактуальну.
> Її сенс — не втомлювати підписників; при 5 підписниках економічний ефект
> нульовий. Тихий нічний режим лишаємо (він безкоштовний і правильний), але
> поріг важливості / зниження MAX_POSTS_PER_RUN зараз не дають нічого.
> Це не докір: 14 сесій пішло на те, щоб пайплайн перестав вигадувати факти —
> просувати канал, який вигадує новини, було б гірше. Але пайплайн уже
> достатньо надійний, і вузьке місце ПЕРЕЇХАЛО з якості на дистрибуцію.
>
> ЦИФРИ РИНКУ (перевірено пошуком 04.08.2026, джерела в чаті сесії):
> • Поріг монетизації Telegram Ads — від ~1000 підписників; реальний заробіток
>   починається на 10k+ (для бізнес-тематики ≈15–30 тис. грн/міс).
> • CPM за тематикою: бізнес/інвестиції 250–400 грн, IT/технології 150–250,
>   освіта 180–300, розваги 50–100 грн за 1000 переглядів. Загальні новини —
>   найконкурентніша й не найдорожча ніша.
> • Фактор №1 у пошуку Telegram — КЛЮЧОВІ СЛОВА В НАЗВІ каналу й username,
>   опис другий за вагою. Українські канали пріоритезуються для укр. юзерів.
>
> ЗВІДСИ ГОЛОВНА ДІРА: канал називається «UA News», username @uanewsbyai —
> жодного українського ключового слова. Українець шукає «новини України»
> кирилицею і цей канал НЕ ЗНАЙДЕ НІКОЛИ. Виправлення безкоштовне і займає
> 5 хвилин, але зробити його треба ЗАРАЗ: зміна username ламає всі старі
> посилання, і при 5 підписниках це нічого не коштує, а при 5000 — коштує дорого.
>
> РІШЕННЯ ВЛАСНИКА (ухвалено в цій сесії): ніша — **загальні новини
> українською** (не англомовні, не вузька вертикаль). Наступний крок —
> план дистрибуції з нуля.
> ЩО ЗРОБЛЕНО ДАЛІ: створено **GROWTH.md** — план дистрибуції під обрану нішу.
> Коротко: Етап 0 — фундамент руками власника (назва каналу з українськими
> ключовими словами, username, опис, закріплений пост, аватар); Етап 1 —
> причина підписатися («✅ Підтверджено N джерелами» — знак довіри з даних,
> які бот уже має в merge_group); Етап 2 — залучення (каталог tglist.com.ua,
> крос-постинг у Bluesky замість платного X, взаємопіар від ~100 підписників).
> Плюс метрики (підписники, перегляди, ER, пересилання) і контрольні точки
> з умовою перегляду ніші на 60-й день.
> ПЕРЕВІРЕНО ПОШУКОМ (04.08.2026): Bluesky AT Protocol API безкоштовний ($0,
> ~11 600 записів/добу, app password) — реальна заміна замороженому X (БАГ-007);
> tglist.com.ua — український каталог, 23 400+ спільнот, є форма додавання;
> фактор №1 пошуку Telegram — ключові слова в назві та username, опис другий.
> КРОКИ ВЛАСНИКА: 1) виконати Етап 0 з GROWTH.md (15 хвилин руками в Telegram;
>   ОСОБЛИВО username — зміна ламає старі посилання, при 5 підписниках це
>   безкоштовно, при 5000 дорого). 2) git add GROWTH.md ROADMAP.md STATUS.md
>   && git commit && git pull --rebase && git push.
> ⏭ НАСТУПНА СЕСІЯ: задача «✅ Підтверджено N джерелами» (GROWTH.md, розділ 5.1)
>   — рядок у format_post_html на основі кількості зведених джерел.
>   Технічний борг (не зник, але не пріоритет): залишок БАГ-012 (129 злитих груп
>   за 7 діб), порожня таблиця feedback, обсяг постів.

> ✅ ЦЯ СЕСІЯ (2026-08-04, 15-а — [OPUS]) — ревізія роботи каналу за добу
> + нічний режим (пости без звуку з 23:00 до 07:00).
> ЗАЧІПАЄ: bot.py (нові QUIET_FROM_HOUR / QUIET_TO_HOUR / is_quiet_hour;
> прапорець disable_notification у post_to_telegram + позначка 🔕 у лог).
> НАСЛІДКИ ЗАПУСКУ: нічого — без нових secret, без зміни cron, без міграцій БД;
> pytz уже є в requirements.txt (ним користується scheduler.py).
>
> РЕВІЗІЯ ЗА ДОБУ (свіжий клон origin + published.db, вікно 03.08 15:25 →
> 04.08 15:25 Київ):
> • 267 постів у канал за добу (попередня доба 247). Прогони кожні ~15 хв,
>   ЖОДНОГО пропуску — ланцюг комітів «update db» безперервний. Дайджести:
>   ранок+вечір 03.08, ранок+war_stats+feed_check 04.08.
> • Дедуп тримає: за 7 діб 0 повторів однакового posted_title серед реально
>   опублікованих (msg_id IS NOT NULL). Відсіяно за добу: duplicate_event 144,
>   duplicate 57, opinion 16, ai_skip 7.
> • ⚠️ ГОЛОВНЕ ЗА ДОБУ: коміт сесії 14 (БАГ-011/012) весь цей час лежав лише
>   локально — бойовий bot.py працював БЕЗ is_bad_output і
>   split_unrelated_groups. Наслідок видно в даних: за добу 61 новина знову
>   загинула в злитих групах (published без msg_id) і 1 пост вийшов із
>   заголовком-стіною > 160 символів. Тобто збиток був реальний, а не
>   теоретичний.
> • ✅ ВИПРАВЛЕНО ПІД ЧАС ЦІЄЇ СЕСІЇ: власник зробив pull --rebase і push —
>   фікс сесії 14 тепер на origin (коміт a6947fe), обидва запобіжники в бою.
>   Ефект перевіряти по логах Actions найближчої доби.
> • УРОК НА МАЙБУТНЄ: закінчена робота ≠ працююча робота. Поки коміт не на
>   origin, GitHub Actions її не бачить. Кожна наступна сесія має ПЕРШИМ ділом
>   звіряти origin з локальною гілкою, а не лише читати STATUS.md.
>
> ЩО ЗРОБЛЕНО (нічний режим): is_quiet_hour() рахує київську годину і в
> тихому вікні post_to_telegram шле disable_notification=True — для sendMessage
> і для sendPhoto. Пост ВИХОДИТЬ як завжди, просто без звуку: він одразу видимий
> у списку чатів з лічильником непрочитаних. У логу Actions такий пост має 🔕.
> ЧОМУ САМЕ ЦЕ: 270 постів/добу = пінг раз на ~5 хвилин цілодобово. Це причина
> №1, чому підписник вимикає сповіщення НАЗАВЖДИ, а мовчазний канал дає мертві
> покази реклами. За 7 діб у вікно 23:00–07:00 потрапило 619 постів із 1875
> (33%) — тобто зміна знімає ~88 нічних пінгів щоночі, не втрачаючи жодної
> новини. Так роблять зрілі новинні канали.
> СВІДОМИЙ КОМПРОМІС: вночі трапляються й критичні новини (масована атака).
> Цей канал — агрегатор, а не система оповіщення; для тривог у людей є окремі
> боти. Вікно змінюється двома числами на початку блоку;
> QUIET_FROM_HOUR == QUIET_TO_HOUR вимикає режим повністю.
> ЗАПОБІЖНИК: будь-який збій із часовою зоною → повертаємо False (постимо зі
> звуком, тобто як було до зміни). Публікація важливіша за тихий режим.
>
> ВЕРИФІКОВАНО (бойові API не смикалися): py_compile bot.py ✅; is_quiet_hour
> прогнано по всіх 24 годинах — тихо рівно 23,0,1,2,3,4,5,6 ✅; payload у
> Telegram із підміненим requests.post: о 03:30 disable_notification=True, о
> 14:30 False, для sendMessage і sendPhoto однаково, chat_id/parse_mode не
> зачеплені ✅; штучний збій pytz → False і виняток назовні не летить ✅;
> QUIET_FROM_HOUR == QUIET_TO_HOUR вимикає режим ✅; diff проти попередньої
> версії адитивний — жодного видаленого рядка, крім двох payload-рядків, які
> лише доповнені ✅; файл лишився UTF-8/CRLF, 0 NUL-байтів ✅.
>
> КРОКИ ВЛАСНИКА:
>   1) git pull --rebase && git push — підняти на origin нічний режим
>      (bot.py + STATUS.md цієї сесії). Конфліктів не очікую: origin за цей
>      час рухав лише published.db.
>   2) Видалити з каналу пости msg 5200 (вигадка про Залужного) і msg 7442 —
>      з минулої сесії, якщо ще не зроблено.
>   3) Через ніч після пушу глянути лог Actions: рядки з 🔕 (нічний режим),
>      «✂️ Розділено групу» і «🚫 Брак від моделі» (запобіжники сесії 14).
>   4) Вранці перевірити телефоном, що вночі канал не будив, а пости на місці.
>
> ⏭ НАСТУПНА СЕСІЯ: (а) ОБСЯГ ПОСТІВ — питання поставлене вже тричі й досі без
>   рішення: 267/добу це ~11 на годину; після пушу фіксу сесії 14 буде ~300.
>   Тихий режим прибрав нічний біль, але денний потік лишається головним
>   ризиком для утримання. Варіанти: поріг важливості в курації (LLM ставить
>   оцінку, публікуємо від N) або зниження MAX_POSTS_PER_RUN. Рекомендація —
>   поріг важливості: він ріже слабке, а не свіже. (б) залишок БАГ-012 —
>   129 груп за 7 діб лишаються злитими, потрібен семантичний чек.
>   (в) таблиця feedback досі порожня (логування живе, повідомлень немає).

> ✅ ЦЯ СЕСІЯ (2026-08-03, 14-а — [OPUS]) — два бракованих пости в каналі:
> знайдено СПІЛЬНИЙ корінь і закрито двома запобіжниками (БАГ-011, БАГ-012).
> ЗАЧІПАЄ: bot.py (нові is_bad_output, split_unrelated_groups, константа
> GROUP_MIN_SIM; два хуки в main() + окремий рядок у підсумку адміну).
> НАСЛІДКИ ЗАПУСКУ: нічого — без нових secret, без зміни cron, без міграцій БД;
> зміни адитивні й ідемпотентні, наявний код не переписувався.
> ЗВІДКИ ПОЧАЛОСЬ: власник показав два пости. (1) 21.07 msg 5200 «Федоров
> залишився лише на одній посаді» — з ВИГАДАНИМ абзацом про чутки щодо
> відставки «головнокомандувача ЗСУ Залужного» і спростування Генштабу та ОП.
> (2) 29.07 msg 7442 — у канал пішла сама ВІДМОВА моделі («Будь ласка, надайте
> повний текст джерела…») як текст новини.
> ЗВІРКА З ДЖЕРЕЛОМ (пост 5200): оригінал ТСН 21.07 14:09 (джерела BBC) —
> Михайло Федоров відмовився від усіх запропонованих посад (радник президента,
> координатор розвитку військових технологій) і розглядає лише повернення
> міністром оборони; у статті згадується конфлікт із СИРСЬКИМ; Залужного в ній
> НЕМАЄ ВЗАГАЛІ, жодного спростування ніхто не давав. У пості неправильно все:
> ім'я («Вадим» замість «Михайло»), сенс («залишився на посаді» замість
> «відмовився від пропозицій»), і повністю вигаданий другий абзац.
> КОРІНЬ (той самий для обох): курація (curate_with_ai) має віддавати «рядок =
> подія», а парсер приймає БУДЬ-ЯКИЙ рядок із цифр і ком. Слабші моделі
> резервного ланцюга регулярно віддають ОДИН плаский список («3,7,1,9,2,5») —
> і 6 різних новин злипаються в одну «подію». Наслідок подвійний: (а) чужа
> новина йде в промпт як «та сама подія іншими словами» → модель змішує факти
> й додумує (кейс 5200: приклеїлась «Під Сирським захиталося крісло»);
> (б) поглинуті новини позначаються published БЕЗ msg_id — тобто гинуть тихо.
> МАСШТАБ (бойова published.db, 27.07–03.08): 564 поглинутих новини за 7 діб
> (~60/добу), з них 77% не мають із «постом-поглиначем» майже жодного спільного
> слова. За 02–03.08 так зникли, зокрема, удар по терміналу «Нової пошти» під
> Харковом із загиблим і стрілянина по військових ТЦК в Одесі.
> ЩО ЗРОБЛЕНО: • is_bad_output(text) — останній запобіжник ПЕРЕД публікацією:
>   мета-фрази моделі, службові позначки («Абзац 1:», «Заголовок:»), <think>,
>   ```, перший рядок > 160 символів, пост < 80 символів, менш ніж половина
>   літер кирилицею. Повертає ПРИЧИНУ (йде в лог і в skipped, reason
>   «bad_output»), нічого не публікує. Раніше перевірялося лише слово SKIP.
> • split_unrelated_groups(items, groups) — перевірка груп курації ЛЕКСИКОЮ
>   перед merge_group: у групі лишаються тільки заголовки, схожі на базовий
>   (Жаккар по _title_words ≥ GROUP_MIN_SIM = 0.15) і без конфлікту міст
>   (_place_conflict). Решта стають ОКРЕМИМИ подіями одразу після своєї групи —
>   не зникають, а йдуть у чергу; не опубліковані цього прогону повертаються
>   кандидатами наступного (published БЕЗ msg_id їм більше не ставиться).
> • bad_cnt у підсумку адміну окремим рядком «🚫 Відсіяно як не-пост: N» —
>   SKIP це редакційне рішення, а брак це збій, їх не можна плутати.
> ЧОМУ ПОРІГ 0.15, А НЕ 0.5: підібрано на реальних даних (564 злиття за 7 діб).
> Нижче 0.15 — різні події («блокада портів» + «удар по терміналу Нової пошти»);
> з 0.15 починаються справжні пари («131 БПЛА» + «107 із 131»). Асиметрія
> помилок навмисна: хибне РОЗДІЛЕННЯ коштує один зайвий виклик LLM (подію
> дожене наявний дедуп по згенерованому заголовку), хибне ЗЛИТТЯ коштує
> вигаданий пост і вбиту новину.
> ВЕРИФІКОВАНО (на копії бойової published.db 03.08, 9826 рядків): py_compile
> bot.py ✅; is_bad_output ловить реальний брак msg 7442, <think>, ```, службові
> позначки, англомовний пост, обрубок, заголовок-стіну ✅; прогін по 4346
> РЕАЛЬНО опублікованих постах — відсіяно 7 (0,16%), і всі сім є справжнім
> браком (msg 7442 + 6 постів, де модель зліпила заголовок з текстом), хибних
> спрацювань 0 ✅; split_unrelated_groups на кейсі 5200 розділяє Федорова й
> Сирського ✅, плаский список із 6 новин → 6 кандидатів ✅, правильні злиття
> (131/107 БПЛА, удар по Львову, витік хімії, втрати за добу) НЕ ламаються ✅;
> прогін по всіх 564 злиттях за 7 діб: 435 новин (77%) було б врятовано, 129
> лишились би злитими ✅; інтеграційний прогін main() з підміненими LLM і
> Telegram: плаский список 1→5 подій, відмова моделі відсіяна в bad_output і
> НЕ опублікована, 3 пости (стеля MAX_POSTS_PER_RUN), жодна новина не
> позначена published без msg_id ✅. Бойові API не смикалися.
> ⚠ ОЧІКУВАНИЙ ПОБІЧНИЙ ЕФЕКТ: обсяг постів зросте. Зараз частина прогонів
> дає 1 пост замість 3 саме через хибне злиття; після фіксу прогони частіше
> впиратимуться в стелю. Орієнтовно 256/добу → до ~300/добу. Це не регрес,
> а повернення вкрадених новин, але рішення по обсягу стає терміновішим.
> КРОКИ ВЛАСНИКА: 1) видалити з каналу пости msg 5200 (вигадка про Залужного —
>   обов'язково) і msg 7442. 2) git add bot.py STATUS.md BUGS.md && git commit
>   -m "Запобіжник проти браку моделі + перевірка груп курації (БАГ-011, -012)"
>   && git push. 3) Через 2–3 години глянути лог Actions: рядки «✂️ Розділено
>   групу», «✂️ Групи курації перевірено», «🚫 Брак від моделі» і новий рядок
>   у підсумку адміну — вони показують, що обидва запобіжники живі.
>   4) Відкрити issue на залишок (див. нижче) і вирішити питання обсягу.
> ⏭ НАСТУПНА СЕСІЯ: (а) залишок по злиттях — 129 груп за 7 діб лишаються
>   злитими, частина з них помилкова через спільну «воєнну» лексику («Російський
>   флот формує полки БПЛА» + «атакували 147 дронами», схожість 0.23); лексика
>   тут уперлася в стелю, потрібен фокусований семантичний чек (можна
>   перевикористати підхід is_semantic_duplicate); (б) рішення по обсягу постів
>   (поріг важливості в курації / MAX_POSTS_PER_RUN); (в) прочитати таблицю
>   feedback (за сесію 13 так і не дивилися).


> ✅ ЦЯ СЕСІЯ (2026-07-20, 13-а — [OPUS]) — бойова перевірка №2 issue #4 (ПРОЙДЕНА)
> + логування фідбеку підписників у БД.
> ЗАЧІПАЄ: feedback_bot.py (нова таблиця feedback у published.db). НАСЛІДКИ ЗАПУСКУ:
> нічого (без нових secret/cron; воркфлоу НЕ чіпав — published.db уже комітиться й
> уже в обробці конфліктів; таблиця feedback створюється сама при першому прогоні).
> БОЙОВА ПЕРЕВІРКА №2 (свіжа published.db 20.07, вікно 19.07 11:17 → 20.07 11:17 UTC):
> • 240 постів у канал за добу; розклад РІВНИЙ, без жодного провалу по годинах
>   (зовн. тригер живий); дайджести ранок+вечір 19.07, ранок+war_stats+feed_check 20.07.
> • 🟢 ДЕДУП ПРАЦЮЄ: 0 жорстких дублів (Жаккар≥0.6 по posted_title серед РЕАЛЬНО
>   опублікованих, msg_id IS NOT NULL) — проти багатьох 19.07 (Wildberries ×6 тощо).
>   За добу відсіяно фільтрами: duplicate_event 186, duplicate 54, opinion 7, ai_skip 3.
> • ЗАЛИШОК #4 (не закриваю): подія «судно біля Одеси / GOLDEN LEO» вийшла ~3× різними
>   формулюваннями через РІЗНІ прогони з ростом жертв 5→10 (msg 4759/4773/4871) —
>   класичний крос-прогонний перефраз+оновлення з низьким лексичним перекриттям.
> • ⚠ ПАСТКА В ДАНИХ (нотатка на майбутнє): рядки published з msg_id IS NULL — це
>   «сусіди» злитих груп (усі URL-и події), а НЕ пости в каналі. Дублі рахувати ЛИШЕ
>   по msg_id IS NOT NULL, інакше повно хибних спрацювань (сам на це наступив).
> ФІДБЕК-ЛОГ: feedback_bot.py тепер пише кожне ТЕКСТОВЕ повідомлення підписника в
> таблицю feedback (published.db): колонки update_id(PK)/user_id/username/name/text/
> received_at; INSERT OR IGNORE по update_id = ідемпотентно (повторний прогін не
> дублює). Зберігаємо ВСЕ за весь час (обсяг мізерний, цінний сигнал). Мета — щоб я
> бачив фідбек у сесіях без ручного скидання власником. Запис іде ДО пересилання
> адміну (не загубиться навіть при збої Telegram).
> ВЕРИФІКОВАНО: py_compile feedback_bot.py OK (повний перезапис UTF-8/LF, 0 NUL-байтів);
> на КОПІЇ published.db — таблиця створюється, вставка працює, повтор update_id → 1
> рядок (ідемпотентність), анонім без username → ім'я/«Анонім», published (4971) не
> зачеплено. Бойовий getUpdates не смикав (треба FEEDBACK_BOT_TOKEN — прогін власника).
> КРОКИ ВЛАСНИКА: 1) git add feedback_bot.py STATUS.md && git commit -m "Фідбек-лог у
>   БД + бойова перевірка №2 issue #4" && git push. 2) Після найближчого прогону —
>   написати щось у фідбек-бот (краще з іншого акаунта); наступної сесії я прочитаю
>   таблицю feedback і покажу. 3) Рішення по обсягу: 240 постів/добу лишаємо чи ріжемо?
> ⏭ НАСТУПНА СЕСІЯ: (а) семантичний дедуп для крос-прогонних перефразів події (кейс
>   «судно») — останній залишок #4; (б) за рішенням власника зниження обсягу постів
>   (поріг важливості в курації / MAX_POSTS_PER_RUN); (в) прочитати таблицю feedback.


> ✅ ЦЯ СЕСІЯ (2026-07-19, 12-а — [FABLE]) — дедуп 2-го рівня: по ЗГЕНЕРОВАНОМУ
> заголовку ДО публікації (issue #4, після проваленої бойової перевірки — розбір
> у блоці сесії 11 нижче).
> ЗАЧІПАЄ: bot.py (нова is_semantic_duplicate; main — перевірка headline перед
> post_to_telegram + skip усієї групи). НАСЛІДКИ ЗАПУСКУ: нічого (без нових
> secrets/cron; +≤3 маленькі LLM-виклики на прогін, save_strong → резерви).
> ЧОМУ: фікс сесії 11 блокує лише URL-и, злиті в групу НА МОМЕНТ публікації;
> нове видання пише про подію в наступному прогоні → його URL «новий»,
> RSS-заголовок лексично інший, промпт-правило курації системно не тримає.
> ЩО ЗРОБЛЕНО: • is_semantic_duplicate(conn, headline): фокусований виклик
>   «кандидат vs 60 опублікованих заголовків доби» (~1,2 тис. токенів,
>   temperature=0, save_strong=True; відповідь ДУБЛЬ N / ОК; fail-open при
>   429/смітті — публікуємо як раніше, прогін не ламається) • у main() перед
>   post_to_telegram: headline тепер обчислюється ДО публікації і проганяється
>   через is_duplicate_title (безкоштовно, ловить близькі формулювання нашої ж
>   моделі) і, якщо та мовчить, — is_semantic_duplicate • дубль → mark_skipped
>   "duplicate_event" для ВСІХ URL-ів злитої групи (не палимо виклики повторно).
> ВЕРИФІКОВАНО: py_compile ✅; на реальних дублях 19.07 з бойової БД: лексика по
>   headline ловить «поїзд Запоріжжя» (4374 vs 4377, Жаккар 0.71) і «бронзу ЧС»
>   (4548 vs 4551 — кейс одного прогону) ✅; семантика (мок call_llm) ловить
>   перефраз Wildberries (4355 vs 4435) ✅; нова подія проходить ✅; 429 і
>   сміття від моделі → fail-open ✅; порожня доба → LLM не викликається ✅.
>   Ліміти: Groq free до 14,4 тис. зап./день на модель, перевірка йде на
>   резерви — запас великий. Бойова перевірка — лише після push.
> КРОКИ ВЛАСНИКА: 1) git push. 2) Через кілька годин глянути канал/логи Actions
>   (рядки «⏭ Семантичний дубль» і «⏭ Дубль події (по заголовку поста)»):
>   повтори типу Wildberries/поїзд мають зникнути. 3) Якщо чисто — закрити #4.
> ⏭ НАСТУПНА СЕСІЯ: бойова перевірка №2 по свіжій published.db (без кешу t.me);
>   за успіху — перенос розбору #4 у BUGS.md і закриття issue.

> ✅ СЕСІЯ (2026-07-18, 11-а — [FABLE]) — дедуп на рівні ПОДІЇ (18.07 «7 ракет / 90 дронів» вийшла 6 разів за 1,5 год).
> ЗАЧІПАЄ: bot.py (init_db, mark_published, is_duplicate_title, curate_with_ai, merge_group, main). НАСЛІДКИ ЗАПУСКУ: нічого (міграція БД автоматична, ідемпотентна).
> КОРЕНІ ДУБЛІВ 18.07 (два):
> • 🔴 ГОЛОВНИЙ: після злиття події з N джерел published позначався ЛИШЕ базовий URL —
>   URL-и решти джерел лишалися «новими» і наступний прогін публікував ту саму подію
>   знову з іншим базовим джерелом. Тому та сама атака виходила щопрогону.
> • RSS-заголовки різних видань про одну подію лексично різні («ППО знешкодила ракету і
>   69 дронів» vs «Ворог запустив 7 ракет та 90 БпЛА») — Жаккар < 0.5, дедуп не ловив.
> ЩО ЗРОБЛЕНО: • mark_published тепер позначає ВСІ URL-и злитої групи (merge_group віддає
>   m["group"]) • нова колонка published.posted_title — ФАКТИЧНИЙ заголовок каналу
>   (перший рядок поста); is_duplicate_title порівнює і з ним, і з RSS-заголовками всієї
>   групи (сітка з ~5 заголовків на подію замість 1) • курація бачить у «ВЖЕ ОПУБЛІКОВАНО»
>   канальні заголовки (COALESCE(posted_title,title)) + явне правило: оновлення події
>   (уточнені цифри, «атака триває», ранкове зведення тієї ж атаки) = ТА САМА подія
>   • INSERT у published — з явними іменами колонок (не залежить від порядку ALTER).
> ВЕРИФІКОВАНО: py_compile ✅; міграція на КОПІЇ бойової published.db ✅; сценарій 18.07:
>   URL-и всіх 4 джерел групи блокуються ✅, варіанти NV/«24 канал» ловляться лексично
>   (0.78) ✅, 4 РІЗНІ події НЕ з'їдаються як дублі ✅. Складний перефраз (Цензор «19
>   влучань») лексика не ловить — його відсікають URL-дедуп групи або курація.
> МЕЖА РІШЕННЯ (чесно): дубль у МЕЖАХ ОДНОГО виклику курації (модель поклала одну подію
>   в дві групи) тримається лише на промпті — якщо повториться, наступний крок:
>   перед публікацією 2-го/3-го поста прогону порівнювати ще й з заголовками щойно
>   опублікованих постів цього ж прогону (вони вже в БД через mark_published — має
>   працювати автоматично; перевірити в бою).
> ЩЕ В ЦІЙ СЕСІЇ (продовження): атрибуція Google News + фільтр opinion-колонок.
> • Джерело «Google News» (пости 4336/4340): назва реального видання тепер береться з
>   тегу <source> запису GN RSS (без HTTP; перевірено feedparser'ом на зразку формату).
> • resolve_gnews_url(): best-effort розгортання redirect-URL до прямої адреси статті
>   (редірект → data-n-au → перший не-Google <a href>); не вийшло — лишається GN-URL,
>   поведінка як раніше. Дає читачу прямий лінк + працюючий fetch_article_text + дедуп
>   за реальним URL. Усі 4 гілки перевірено на мок-відповідях; ЖИВУ заглушку Google
>   з пісочниці перевірити неможливо (домен заблоковано) — див. КРОКИ ВЛАСНИКА.
> • Фільтр opinion (пост 4331 — колонка NV як факт): is_opinion_url за маркерами шляху
>   (/opinion/, /blogs/, /columns/, /dumka/…) → mark_skipped(reason="opinion").
>   Перевірено на реальному URL 4331 (ловиться) і 4 звичайних новинних (проходять).
> • Нюанс: старі записи published містять GN-URL, нові — прямі; одна й та сама стаття
>   в перехідну добу може не зматчитись за URL — страхують posted_title і курація.
> КРОКИ ВЛАСНИКА: 1) Якщо ще не видалив: у PowerShell
>   `del .git\index.lock, .git\HEAD.lock, .git\objects\maintenance.lock`. 2) git push.
> 3) Через кілька годин перевірити канал: масована атака = 1 пост, не 6; у постів із
>   Google News має бути назва реального видання; в логах Actions глянути, чи
>   resolve_gnews повертає прямі URL (рядки «⚠️ resolve_gnews» = фолбек працює).
> 4) Якщо дублі зникли — закрити issue #4.
> ЩЕ В ЦІЙ СЕСІЇ (2): вигаданий пост «Кабмін спростував відставку Федорова» (18.07,
> знайшов власник) — у статті за посиланням нічого подібного. Корінь: коли матеріалу
> жменя (стаття не дісталась, анонс куций), а формат вимагає «2-3 змістовні абзаци»,
> модель добудовує решту з фантазії — аж до вигаданих заяв пресслужб.
> • rewrite_with_ai: обсяг поста тепер АДАПТИВНИЙ — material_len (анонс+стаття+extra)
>   < 400 симв. → вимога «ОДИН короткий абзац строго з наявних фактів, бракує — SKIP»;
>   ≥ 400 → звичайні 2-3 абзаци. • У блок ТОЧНІСТЬ додано пряму заборону вигаданих
>   ПОДІЙ-РЕАКЦІЙ (заяв, спростувань, підтверджень, коментарів пресслужб).
> ВЕРИФІКОВАНО: py_compile ✅; обидві гілки промпта рендеряться правильно (куций
>   матеріал → короткий формат, повний → звичайний; заборона в обох) — мок call_llm.
>   Чи перестане СЛАБКА модель вигадувати — перевіряється лише в бою.
> ⚠ Баг #1 НЕ закритий: це зниження ймовірності, не гарантія. Наступний рівень,
>   якщо повториться — верифікаційний LLM-прохід «пост проти джерела» (дорого:
>   +1 виклик на пост) або SKIP усіх новин без повного тексту статті (втрата обсягу).
> ЩЕ В ЦІЙ СЕСІЇ (4): фільтр езотерики (у канал зайшла «шаманка прогнозує перелом у
> війні», пост 4346, УНІАН lite/astrology). SPAM_KEYWORDS += шаманк/астролог/гороскоп/
> екстрасенс/…; OPINION_MARKERS += /astrology/, /horoscope/, /goroskop. Перевірено на
> реальному URL 4346 (ловиться обома шарами) і 3 звичайних новинах (проходять).
> РАННІЙ ОГЛЯД ПІСЛЯ PUSH: атрибуція GN ПРАЦЮЄ (пост 4354 — «Rubryka» замість «Google
> News»; прямий URL Google не віддав — фолбек, як задумано). Дублі ще спостерігались
> (Херсон-авто ×4: 4339/4342/4345/4351; Зеленський ×2: 4353/4357; Флеш ×2: 4347/4358),
> але межа старий/новий код нечітка — точний вердикт дасть вечірня перевірка 21:30.
> ⚠ БОЙОВА ПЕРЕВІРКА (2026-07-19 ~04:10 UTC, планова задача [FABLE]) — НЕ ПРОЙДЕНА,
> issue #4 НЕ закрито. Метод: свіжа published.db з origin/main (коміт 04:02 UTC),
> 265 постів після push фіксу (msg_id 4347–4614); t.me/s віддавав кеш від 12–13.07
> (пости №3094–3113), Chrome недоступний — тому аналіз за posted_title з БД.
> ДУБЛІ ТРИВАЮТЬ, приклади (msg_id, час UTC 18–19.07):
> • удар по поїзду в Запорізькій обл.: 4368/4374/4377/4380 (09:46–10:31) — ×4 за 45 хв;
> • Wildberries: 4355/4434/4435/4448/4454/4463 — ×6 за день (4434 і 4435 — 14 хв різниці);
> • Зеленський + командири корпусів: 4401/4403 (3 хв різниці)/4414; Федоров-Сирський
>   консультації: 4485/4488/4495/4496 — ×4 за годину;
> • прапор у Костянтинівці: 4399/4409/4416; Краматорськ (3 загиблих): 4456/4468/4499;
> • парк розваг на Одещині: 4477/4503/4507; Херсон, дрон у багатоповерхівку: 4494/4502/4515;
> • матч за «бронзу» ЧС: анонс 4548/4551 (23:11/23:16 — 5 хв, імовірно ОДИН прогін —
>   підтверджує кейс «МЕЖА РІШЕННЯ»), результат 4557/4597;
> • балістика по Києву: 4544/4550/4559/4568/4571/4574/4585/4603 — 8 постів за ніч
>   (частина — «оновлення», але за правилом сесії 11 оновлення = ТА САМА подія).
> ГІПОТЕЗА ПРИЧИНИ: фікс блокує лише URL-и, які ВЖЕ були злиті в групу на момент
> публікації. Інше видання пише про ту саму подію в наступні прогони → його URL
> у published відсутній, заголовок лексично інший (Жаккар < 0.5) → лишається тільки
> курація, і вона систематично пропускає. Окремо підтверджено дубль у межах одного
> прогону (4548/4551). НАПРЯМ ФІКСУ: семантичний дедуп проти posted_title за останні
> 24 год (LLM-звірка кандидатів зі списком опублікованого вже Є в курації — отже,
> промпт-правило не працює; потрібен окремий дешевий крок звірки або ембединги).
> АТРИБУЦІЯ GN / OPINION: у каналі перевірити не вдалося (кеш); за раннім оглядом
> 18.07 атрибуція працює (пост 4354 «Rubryka»); у published URL не зберігається.
>
> ЩЕ В ЦІЙ СЕСІЇ (3): фільтр зборів коштів (рішення власника: категорично без них).
> • FUNDRAISER_MARKERS + is_fundraiser(): лише однозначні словосполучення («збір на»,
>   «оголосив збір», «збирають кошти», send.monobank…) — слово «збір» саме по собі
>   НЕ маркер (податковий збір, збірна, збір урожаю). mark_skipped(reason="fundraiser").
> • Другий шар: у SKIP-правилах промпту переписування — збір/реквізити в тексті
>   статті → SKIP (на випадок, коли в заголовку/анонсі збору не видно).
> ВЕРИФІКОВАНО: py_compile ✅; 4 реальні збори ловляться (вкл. кейс NV «Рубіж»),
>   5 звичайних новин зі словами «збір/збирає/збірна» проходять ✅.
> КРОКИ ВЛАСНИКА (додатково): видалити з каналу вигаданий пост про «спростування
>   Кабміну» і пост зі збором NV вручну (я бойовий API не чіпаю).
> ⏭ НАСТУПНА СЕСІЯ: за результатами бойової перевірки — ревізія resolve_gnews (якщо
>   Google не віддає прямий URL — розглянути batchexecute-API або прибрати спробу);
>   баг #1 (якість переказу, моніторити після сьогоднішнього фіксу) і #3 (погода в
>   ранковому дайджесті) лишаються відкриті.

> ✅ ЦЯ СЕСІЯ (2026-07-16, 10-а — [Code/Fable]) — ревізія за добу: інфраструктура здорова, знайдено корінь дублів-перефразів (#4) і погоди (#3).
> ЗАЧІПАЄ: bot.py (курація+дедуп), digest.py (погода, parse_mode). НАСЛІДКИ ЗАПУСКУ: нових secret нема; поведінка — менше дублів, погода по всіх містах.
>
> РЕВІЗІЯ ЗА ДОБУ (15.07 11:00 → 16.07 11:00):
> • Інфраструктура: усі ~100 прогонів зелені (зовн. тригер кожні 15 хв працює), 28/28 фідів живі,
>   war_stats/дайджести/feed_check відпрацювали щодня (daily_log повний). БД зберігається (pull --rebase діє).
> • ОБСЯГ: 317 постів/добу (~13/год) — бот постить 3/прогін майже щоразу. Це МАКСИМУМ конвеєра;
>   чи не забагато для підписників — рішення власника (див. «ПИТАННЯ ВЛАСНИКУ»).
> • ДУБЛІ (#4) підтверджено масово: «ЦРУ: 20-30 хв» ×3, «21-й пакет санкцій» ×7 варіацій, Мі-28 ×3,
>   «Посли ЄС захист українців» ×5; той САМИЙ заголовок заходив повторно через Google News (інший URL).
>
> ЗНАЙДЕНІ КОРЕНІ І ФІКСИ (закомічено f15689b, 600c988; перевірено бойовим прогоном 29484187129 ✅):
> • 🔴 Курація ЗАВЖДИ йшла на найслабшій gemma-31b: max_tokens=400 спалювався reasoning'ом gpt-oss-120b
>   → finish_reason=length → Groq відпадав. У логах це видно як «обірвано» ПЕРЕД кожною курацією. Тепер 1600.
> • 🔴 Вікно дедупу курації: LIMIT 25 заголовків = ~2 год історії при 300/добу — ранкові події ввечері
>   «невидимі». Тепер 60. + Лексичний is_duplicate_title повернуто і на AI-шлях (хибних злиттів міст
>   не буде — _place_conflict блокує; страхує від повторів через Google News).
> • 🔴 Погода (#3): корінь — НЕ 429, а ReadTimeout на серії з 29 послідовних запитів. Тепер ОДИН
>   batch-запит (координати через кому) — перевірено живим викликом: 29/29 міст з даними.
> • digest.py: прибрано parse_mode=Markdown (текст пише LLM; випадкові _ * [ валили б увесь дайджест).
> ⚠ Groq майже весь день у 429 (добовий ліміт з'їдають ~300 переписувань) → і письмо, і курація на gemma;
>   звідси одруки в постах («дешею», «всіій»). Зменшення обсягу → менше викликів → частіше сильна модель.
>
> 📋 ПИТАННЯ ВЛАСНИКУ: (а) 317 постів/добу — лишити чи знизити (напр. MAX_POSTS_PER_RUN 3→2 або
>   поріг важливості в курації)? Це і якість підніме (менше палимо ліміт Groq). (б) У закріпленому
>   пості досі рядок «🐦 Ми в X: @LiveUkrNews» — Twitter заморожено, варто прибрати (пункт з сесії 9).
>
> ДОДАНО ПІЗНІШЕ ЦІЄЇ Ж СЕСІЇ (рішення власника: «якість — так, але ліміти не повинні простоювати»):
> • 🧠 Розумний розподіл лімітів (коміт dd47976, call_llm save_strong): обсяг НЕ ріжемо. Курація і
>   ПЕРШИЙ (найважливіший) пост прогону → Groq(120B)→Cerebras→Gemini; 2-й/3-й пости → Cerebras→Gemini→Groq.
>   Groq тепер ~200 викликів/добу замість ~400 (не вигорає зранку), ліміти Cerebras/Gemini працюють.
> • Перевірено прогоном 29486172622 ✅: 3 пости, і лексичний запобіжник УПЕРШЕ спрацював у бою —
>   «Британія розробить балістичні ракети» відсіяно як дубль (постили о 11:15 з іншим URL).
>
> ⚠ ІНЦИДЕНТ (обрив живлення 16.07 ~11:54, під час git pull --rebase): NTFS занулив щойно записані
>   файли — .git/HEAD, index, refs/remotes/origin/*, ORIG_HEAD/FETCH_HEAD/COMMIT_EDITMSG і САМ bot.py
>   (52КБ суцільних NUL). ВРЯТУВАЛО те, що коміт встиг створитись: об'єкти git уціліли, bot.py відновлено
>   `git checkout -- bot.py`. РЕЦЕПТ на повтор: HEAD → `ref: refs/heads/main`; rm битих index/refs/rebase-merge;
>   git fetch + remote set-head; git fsck; робочі файли перевіряти на NUL-байти (grep може NUL не показати —
>   надійніше python: src.count(b'\\x00')). Комітити ПЕРЕД pull — незакомічене було б втрачено назавжди.
>
> ЩЕ ПІЗНІШЕ ЦІЄЇ Ж СЕСІЇ (фідбек власника по якості контенту):
> • ТИЗЕРИ БЕЗ СУТІ (кейс «НБУ відповів на чутки про 5000 грн», пост 15.07 22:31 — переказ без відповіді):
>   спершу дав вузьке SKIP-правило (620c37a), але власник слушно спитав «а чого не піти почитати суть?» →
>   b74ecae: fetch_article_text() тягне ПОВНИЙ текст статті (до 3500 симв., <p> з <article>, фолбек на
>   анонс) — перевірено на тій самій статті ТСН, суть дістається. SKIP-тизер лишився останнім запобіжником.
>   Бонус: повніші пости для всіх новин. Пильнувати: чи не тягне модель сміття сайту в пости.
> • ВЕЧІРНІЙ ДАЙДЖЕСТ «сухий» (фідбек власника): 929c8a1 — тепер топ-5 з ЕМОДЗІ і прямим посиланням
>   «→ пост» на повний пост каналу під кожним пунктом. Для цього published отримала колонку msg_id
>   (ALTER у init_db, міграцію перевірено), post_to_telegram повертає message_id. Модель віддає
>   «номер|речення», посилання підставляє КОД (LLM не може переплутати URL). Запасні шляхи: старий
>   формат без посилань (модель не втримала формат) і plain-повтор (Telegram відхилив HTML).
>   ⚠ ПЕРЕВІРИТИ ввечері 16.07 о 21:00 реальний вигляд підсумку — верифікації в бою ще НЕ БУЛО.
>
> НОВІ ПРОВАЙДЕРИ (запит власника «підключити ще безкоштовних моделей», коміт ecab937):
> • GitHub Models: gpt-4.1-mini через ТОКЕН САМОГО WORKFLOW (permissions: models: read + github.token) —
>   жодних нових секретів. Low-tier: 150 запитів/добу, 15/хв, ~8k токенів вхід (наші промпти вміщаються).
>   Позиція №2 у ланцюзі (top). Бойовий тест: auth ОК (одне 500 «overloaded» — транзієнт Azure).
> • Mistral La Plateforme (Experiment): 1 МЛРД токенів/міс — майбутній робочий кінь замість gemma.
>   Код готовий (mistral-large-latest), чекає секрет MISTRAL_API_KEY від власника. Після додавання:
>   перевірити логи — якщо large на free недоступна (401/403), змінити на mistral-medium-latest.
> • NVIDIA NIM (переперевірено після питання власника — перший відкид був за застарілими даними):
>   кредитні ліміти ПРИБРАНО у 2026 — тариф безлімітний за обсягом, стеля лише 40 зап./хв (нам вистачає).
>   Каталог публічний (118 моделей): deepseek-v4-pro/flash, mistral-large-3-675b, nemotron-ultra-253b.
>   Підключено deepseek-v4-flash (фронтир без «міркувань», вкладається у timeout 30с); чекає секрет
>   NVIDIA_API_KEY (build.nvidia.com, Developer Program, лише email). Коміт c9e0265.
> • Ланцюг тепер: Groq(top) → GitHub(top) → Mistral → NVIDIA → Cerebras → Gemini; save_strong відсуває top-и в кінець.
> • ✅ ПІДТВЕРДЖЕНО В БОЮ (прогін 29513032158, ключі власник додав 16.07): «Моделі: GitHub×2, Mistral×3» —
>   gpt-4.1-mini взяв курацію+топ-пост, mistral-large-latest ПРАЦЮЄ на безкоштовному плані і веде добивку.
>   gemma тепер аж 5-та в черзі. Стежити: чи зникли одруки; NVIDIA підхопить, коли Mistral спіткнеться.
> • ⚠ Гонка збереження БД (прогін 29512659665 згорів, 3 пости 15:47 UTC втрачені з БД → можливі їх дублі):
>   виправлено aa06367 — checkout ref: main (не пришпилений SHA) + retry тепер вирішує конфлікт
>   бінарної БД (--theirs у rebase = наша) або робить rebase --abort перед повтором.
> • Відхилені варіанти: OpenRouter (200 зап./добу — мало), Cohere (1000 зап./МІСЯЦЬ),
>   HuggingFace ($0.10/міс кредитів — сміховинно).
>
> ⏭ НАСТУПНА СЕСІЯ: (а) вечірній підсумок 16.07 21:00 — посилання/вигляд (перша бойова перевірка);
>   (а2) у фідбек-звітах шукати «GitHub×N» — чи включився gpt-4.1-mini; після MISTRAL_API_KEY — «Mistral×N»;
>   (б) подивитись дублі за добу ПІСЛЯ фіксів (той самий SQL по published + t.me/s) — перефрази у межах
>   одного прогону досі проскакують («Рада призначила Корецького» ×2 о 12:16, Жаккар 0.375<0.5) →
>   наступний важіль: стисла історія за 24 год для курації, issue #4; (в) ранковий дайджест 17.07 —
>   чи всі міста з погодою (закрити issue #3); (г) баланс «Groq×N, Cerebras×M» у підсумках адміна;
>   (д) якість постів З повним текстом статті — чи не тягне модель сміття сайту.

> ✅ ЦЯ СЕСІЯ (2026-07-15, 9-а — [Code/Opus]) — канал ожив: знайдено корінь мовчання, ШІ-курація, джерела.
> ЗАЧІПАЄ: bot.py (головне), digest.py, scheduler.py, war_stats.py, twitter_bot.py, news_bot.yml, BUGS.md, ROADMAP.md.
> НАСЛІДКИ ЗАПУСКУ: нових secret немає; Twitter лишається вимкненим; поведінка каналу помітно змінилась (див. нижче).
>
> ЩО ЗРОБЛЕНО (у порядку важливості):
> • 🔴 КОРІНЬ «МОВЧАННЯ КАНАЛУ» (БАГ-010): коміт a74df05 обрізав хвіст bot.py — зник виклик
>   `if __name__ == "__main__": main()`. Файл лишався валідним Python, помилок не було — бот просто
>   нічого не робив. Відновлено; канал одразу почав постити.
> • 🔴 ВТРАТА СТАНУ (корінь дублів + 429): крок «Save database» робив git push БЕЗ pull --rebase → щойно
>   в main з'являвся чужий коміт, push падав і published.db НЕ зберігалась. Бот годинами працював зі старою
>   БД: не знав про опубліковане (→ дублі) і про SKIP (→ ті самі новини знову в LLM → 429 на всіх
>   провайдерах). Тепер: pull --rebase + retry×3 + явна помилка в лог.
> • 🧠 ШІ-КУРАЦІЯ (заміна лексичних алгоритмів — ідея власника): один виклик LLM бачить опубліковане за добу
>   + усіх кандидатів, повертає лише номери (рядок = подія, номери = джерела про неї). Дедуп, злиття джерел,
>   вибір важливого — усе одним викликом. 12 викликів → 4. Fallback на лексичний шлях при 429/смітті.
> • 📰 ДЖЕРЕЛА: «За даними: X, Y» — кожне джерело ПРЯМЕ посилання на свою статтю. «Читати повністю» прибрано
>   (дублював перше джерело). Назви задані ЯВНО в RSS_FEEDS (feed.title довільний: Укрінформ віддає «Останні
>   новини», Цензор — «Цензор.НЕТ - Новини», Європравда — нічого).
> • 🌍 СВІТОВІ НОВИНИ не потрапляли в канал: ukraine_score виштовхував en-джерела за межі топ-12 ще до курації.
>   Тепер резерв: 9 укр + 3 світові. Перевірено — Reuters з'явився в каналі.
> • 🎨 СТИЛЬ NV + HTML: жирний заголовок з доречним емодзі + короткі абзаци; parse_mode Markdown→HTML
>   (Markdown валився на символах _ * [ у сирому тексті новини).
> • ✂️ ОБІРВАНІ ПОСТИ («...дворічну підтрим»): gpt-oss-120b — reasoning-модель, палила max_tokens на
>   «міркування». Тепер call_llm відкидає finish_reason=length і віддає наступному провайдеру; max_tokens 900→1600.
> • SKIP-кеш (таблиця skipped) — відхилені новини не ганяються через LLM повторно.
> • scheduler/digest/war_stats: mark_today лише при успішному коді виходу (дайджест більше не «губиться» на добу).
> • Twitter ЗАМОРОЖЕНО остаточно: X скасував безкоштовний API (pay-per-use, $0.20/твіт з посиланням).
>
> ⚠️ МОЇ ПОМИЛКИ ЦІЄЇ СЕСІЇ (щоб не повторити):
> • Додав у промпт «краще SKIP, ніж порожній однорядок» → модель почала відхиляти ~70% новин. Поки бот
>   перебирав 12 кандидатів, це маскувалось; після курації (пул 3) канал видав 0 постів. Прибрано.
> • Змінив структуру sources на {name,url} і пропустив одне місце → TypeError → бот падав, не публікуючи.
>   Знайдено власним прогоном ДО того, як побачив власник.
> • ВИСНОВОК (вимога власника, тримати завжди): ПЕРЕВІРЯТИ на реальних логах/каналі ПЕРЕД тим, як казати
>   «працює» і перед наступною зміною. Юніт-тести чистих функцій НЕ ловлять регресій промпту/LLM.
>
> 🛠 ЯК ПЕРЕВІРЯТИ (ключове для наступних сесій):
> • Логи прогонів: `gh run list --repo blazhkevych/telegram-news-bot`, `gh run view <id> --log` → грепати
>   маркери: 📥 Знайдено / 🧠 Курація AI / ⏭ AI пропустив / 🏁 Опубліковано / Traceback.
> • Ручний запуск: `gh workflow run news_bot.yml --ref main` (крон GitHub часто затягує).
> • Канал видно самому: браузер → https://t.me/s/novyny_ua_svit (адресу оновлено 08.08, було /s/uanewsbyai; старі записи про «пісочниця не має мережі» — неактуальні).
> • gh CLI встановлено (winget). Токен git-credential має scope repo/workflow, але БЕЗ read:org, тому
>   `gh auth login` не проходить — працює лише інлайн: `GH_TOKEN=<токен> gh <команда>`.
>
> 📋 ПРОЦЕС: відкриті задачі — у GitHub Issues (#1 якість переказу, #3 погода, #4 дублі-перефрази + перекіс).
>   BUGS.md — архів виправленого. ROADMAP.md — стратегія. STATUS.md — цей журнал.
>
> ⏭ НАСТУПНА СЕСІЯ:
> (а) ДУБЛІ-ПЕРЕФРАЗИ (issue #4) — головне невирішене: курація ловить не все («Посли ЄС» вийшли двічі).
> (б) Перевірити, чи модель тепер зберігає точні назви (орден/номер закону) — промпт оновлено, результат не бачив.
> (в) Погода (issue #3) — діагностику додано, чекаємо лог ранкового дайджесту (чи це 429 Open-Meteo).
> (г) Власнику: у закріпленому пості прибрати рядок «🐦 Ми в X: @LiveUkrNews» — Twitter заморожено.
> (д) Нюанс: посилання на Reuters/AP ведуть через news.google.com (редірект), бо їх тягнемо через Google News.

> ✅ ЦЯ СЕСІЯ (2026-07-14, 8-а — [Code/Sonnet]) — критичний фікс bot.py (main() не викликався) + дрібні баги; GitHub тепер веду сам.
> ЗАЧІПАЄ: bot.py, digest.py, scheduler.py, war_stats.py, twitter_bot.py, BUGS.md, ROADMAP.md.
> НАСЛІДКИ ЗАПУСКУ: нових secret немає; наступний прогін cron має знову почати постити.
> ЩО ЗРОБЛЕНО:
> • КРИТИЧНО: коміт a74df05 (сесія 7) обрізав хвіст bot.py — зник виклик
>   `if __name__ == "__main__": main()`. Файл валідний синтаксично (не падав з помилкою),
>   тому просто виконувався і одразу завершувався, НІЧОГО не роблячи — жодних постів,
>   жодних помилок у логах. Це, найімовірніше, справжня причина «порожнього каналу»
>   з 14.07 (а не лише застарілі RSS-адреси з БАГ-009). Відновлено повністю, БАГ-010.
> • bot.py: `is_russian(title, title)` → `is_russian(title, summary)` — фільтр
>   російської мови раніше не перевіряв текст новини, лише заголовок (двічі).
> • bot.py: прибрано мертвий gate «чекати підтвердження теми» (`get_topic_count < 1`
>   у main()) — лічильник теми інкрементувався ще в fetch_news() до цієї перевірки,
>   тож умова спрацьовувала лише для заголовків без слів довших 5 символів.
> • scheduler.py + digest.py/war_stats.py: `mark_today()` тепер лише при успішному
>   коді виходу скрипта (раніше дайджест/статистика «губились» на добу при падінні —
>   саме той пункт, що висів у «НАСТУПНА СЕСІЯ» з сесії 6/7).
> • twitter_bot.py: додано браузерний UA для RSS (як у bot.py) — окрема копія, бо
>   імпорт з bot.py впав би через відсутні TELEGRAM_* у кроці Twitter-воркфлоу.
> • bot.py: `is_valid_image` — fallback на GET, якщо CDN блокує HEAD (403/405).
> • ROADMAP.md: Z-3 (спам war_stats) і Z-4 (дедуп подій) позначено як фактично вже
>   реалізовані в коді (daily_log guard + is_duplicate_title Жаккар) — ROADMAP не
>   встигли оновити раніше.
> ВЕРИФІКОВАНО: ast.parse на всіх 5 змінених файлів OK; grep підтвердив рівно один
>   виклик `main()` під `__main__` у кожному з bot.py/digest.py/war_stats.py;
>   git diff перед комітом перевірено вручну рядок-в-рядок.
> ⚙ GITHUB (нове, важливо для наступних сесій): у режимі "Code" підтверджено —
>   `git push` працює напряму (credential manager уже авторизований під власником,
>   `origin/main` == локальний HEAD після push). Додатково встановлено `gh` CLI
>   (winget, 2.96.0): `gh auth login --with-token` не проходить (токен git-credential
>   без scope `read:org`, а gh на цьому вимагає його при логіні) — ОБХІД: токен
>   передається як `GH_TOKEN=... gh <команда>` інлайн у кожному виклику (сам токен
>   ніде не друкується і не лежить на диску). Реальні scope токена: `gist, repo,
>   workflow` — issues/PR/workflow дії доступні. ПРЕФЕРЕНЦІЮ (1) нижче оновлено:
>   власник підтвердив — Claude в режимі "Code" веде git/GitHub самостійно.
> 🔎 БАЧУ КАНАЛ САМ (важливо): у режимі "Code" через вбудований браузер
>   (preview_start → https://t.me/s/novyny_ua_svit → get_page_text) видно РЕАЛЬНІ свіжі пости.
>   Старі записи «пісочниця не має мережі, верифікує власник / t.me/s кешується» — НЕ стосуються
>   браузера Claude Code. Тепер сам перевіряю вигляд/якість постів після публікації.
> 🎨 СТИЛЬ NV + HTML (зроблено, коміти e3172d0, 3ff7140): пости тепер — жирний заголовок з доречним
>   емодзі + короткі абзаци; parse_mode Markdown→HTML (надійніше, не валиться на _ * [ у тексті).
>   Формат підтверджено на реальному каналі (пости від 17:01 ✅). Далі підкручено промпт: повніші пости
>   (2-3 змістовні абзаци, SKIP замість однорядка), доречні за тоном емодзі, заборона «води».
> 🐦 TWITTER/X — ЗАМОРОЖЕНО ОСТАТОЧНО (рішення власника 2026-07-14): X скасував безкоштовний API,
>   тепер pay-per-use ($0.015/твіт, $0.20/твіт з посиланням — а бот постить з посиланням). 401 був
>   наслідком вимкнення free-tier, не прав. Суперечить «тільки безкоштовне» → лишили if:false. BUGS-007
>   закрито, issue #2 закрито (not planned). Код обмеження обсягу (MAX_TWEETS_PER_RUN) готовий на майбутнє.
>   Безкоштовна альтернатива на потім — Bluesky/Mastodon. ⚠ У ЗАКРІПЛЕНОМУ ПОСТІ прибрати рядок «🐦 Ми в X:
>   @LiveUkrNews» (обіцяє те, чого нема) — власник постить сам.
> 📋 ПРОЦЕС (нове): відкриті баги/задачі тепер трекаємо в GitHub Issues, НЕ у BUGS.md.
>   Створено issues #1 (БАГ-006 якість), #2 (БАГ-007 Twitter 401), #3 (погода недоступна).
>   BUGS.md перетворено на АРХІВ виправленого (детальні розбори закритих багів). ROADMAP.md —
>   стратегія/пріоритети. STATUS.md — журнал сесій і моя пам'ять між сесіями (лишається головним).
>   Коли закриваєш баг: перенеси розбір у BUGS.md «✅ Виправлені» + закрий issue комітом (closes #N).
> ⏭ НАСТУПНА СЕСІЯ: підтвердити з фідбек-бота, що канал знову постить (перевірити
>   підсумок після найближчого cron-прогону); далі — issue #1 (якість переказу) або
>   issue #3 (погода) — обидва невеликі, дають видимий результат.

> ✅ ЦЯ СЕСІЯ (2026-07-14, 7-а — [OPUS]) — джерела виправлено й розширено (28 RSS); зовн. тригер; тривоги відкладено.
> ЗАЧІПАЄ: bot.py (RSS_FEEDS), feed_diag.py (новий), .github/workflows/feed_diag.yml (новий), EXTERNAL_TRIGGER.md.
> НАСЛІДКИ ЗАПУСКУ: додано зовн. планувальник cron-job.org (діє); нових secret у боті нема.
> ЩО ЗРОБЛЕНО:
> • ДІАГНОЗ уточнено: «мертві» фіди — це НЕ User-Agent і НЕ IP-блок (спростовано через feed_diag на раннері GitHub),
>   а просто ЗАСТАРІЛІ адреси (404/403). БАГ-009 переформульовано й закрито.
> • Новий feed_diag.py + workflow_dispatch-воркфлоу feed_diag.yml: тестує джерела З IP сервера GitHub і шле звіт
>   адміну (HTTP-статус / к-сть записів / причина). Два раунди кандидатів прогнано власником.
> • RSS_FEEDS переписано: 28 ПЕРЕВІРЕНИХ джерел (17 uk / 11 en). Виправлено УНІАН (rss.unian.net/site/news_ukr.rss),
>   Suspilne (/rss/all.rss). Додано: Цензор, LB.ua, Європравда, Google News (укр агрегатор), Guardian, Al Jazeera,
>   Euronews, CNN, і через Google News (site-фільтр) — Радіо Свобода, DW, Reuters, AP. Прибрано мертві:
>   Корреспондент, Mind, ОПОРА, LIGA, Економічна правда, стара radiosvoboda/DW, Громадське/Главком, ZN.ua.
> • Зовнішній тригер (з сесії 6) ДІЄ: cron-job.org → GitHub API workflow_dispatch, TEST RUN = 204, нові прогони йдуть.
> ВЕРИФІКОВАНО: py_compile bot.py+feed_diag.py; import bot → 28 джерел, усі мають url+lang; diag_one прогнано на моках
>   (200/403/timeout/connerr/200-без-RSS); живучість усіх 28 підтверджена звітом feed_diag власника (усі ✅).
> ⚠ ІНЦИДЕНТ (важливо для наступних сесій): цей Windows-маунт КІЛЬКА разів обрізав файли на кирилиці —
>   і Edit-tool, і один точковий Python-replace (feed_diag втратив виклик main → порожній прогон). МІТИГАЦІЯ, якої
>   тримаюсь: писати файли повним перезаписом через bash-Python (newline='\n'), і ПЕРЕВІРЯТИ з диску ОКРЕМОЮ
>   командою (wc -l + grep кінцевих рядків + py_compile), а не покладатись на успіх запису.
> ⚠ ТРИВОГИ — відкладено. Рішення узгоджено: миттєвість обов'язкова → потрібен always-on хост (GitHub пакетний,
>   ~1–2 хв затримки — не годиться). Безкоштовний шлях без картки = старий Android + Termux, увімкнений у зарядку 24/7
>   (опитувати alerts.in.ua кожні ~10–15 с). У власника зараз нема запасного телефону — ставимо на паузу.
> ⚠ ПРЕФЕРЕНЦІЇ ВЛАСНИКА (тримати завжди): (1) [ОНОВЛЕНО 2026-07-14, сесія 8] У режимі
>   "Code" Claude веде git/GitHub САМОСТІЙНО (commit/push/issues) — окреме підтвердження
>   не потрібне щоразу. У Cowork-чаті (де немає прямого доступу до диска/git) лишається
>   старий режим — текст коміту, пушить власник. (2) Логи приймати ФАЙЛОМ (досить одного — вивід кроку діагностики), не копіпастом. (3) Працювати
>   як команда, приймати потрібну експертну роль. (4) НЕ піднімати тему грошей/платних рішень, поки проект не заробить.
>   (5) Пояснювати простими словами, без жаргону; вести STATUS.md щосесії (як у LexControl).
> КРОКИ ВЛАСНИКА: 1) Переконатись, що запушено останні bot.py (28 джерел) і feed_diag.py (коміт-меседжі я давав у чаті).
>   2) Більше нічого — джерела працюють автоматично.
> ⏭ НАСТУПНА СЕСІЯ (новий чат): (а) СТИЛЬ постів у дусі NV — заголовок+емодзі, короткі абзаци, цитата-blockquote з
>   атрибуцією + перехід parse_mode Markdown→HTML з безпечним екрануванням (приклади постів NV власник надсилав).
>   (б) Тривоги — коли з'явиться запасний Android (Termux + alerts.in.ua, безкоштовний токен). (в) Опційно: вимкнути
>   вбудований cron */15 у news_bot.yml (зовн. тригер тепер основний; concurrency робить співіснування безпечним).
>   (г) Спостерігати: digest.py mark_today("morning") при падінні губить дайджест на добу; погода «дані недоступні»
>   для ~10 міст (ліміт Open-Meteo?); БАГ-006 (якість переказу), БАГ-007 (Twitter 401).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 6-а — [OPUS]) — корінь «порожнього каналу»: GitHub дропає крон */15.
> ЗАЧІПАЄ: EXTERNAL_TRIGGER.md (новий). Код/воркфлоу НЕ чіпав.  НАСЛІДКИ ЗАПУСКУ: новий зовн. планувальник + PAT (кроки нижче).
> ЩО ЗРОБЛЕНО:
> • Власник підтвердив: у Actions є Scheduled-запуски, банера про disable нема — крон УВІМКНЕНИЙ. Але
>   реальна каденція сьогодні: 00:42→01:40→02:47→05:11→09:09→13:18→16:15 — діри по 3–4 год. GitHub
>   деприоритезує/дропає високочастотні крони (`*/15`). Звідси мало новин і «сповзання» дайджестів.
> • Пояснено «Доброго ранку!» о 19:00: ранковий дайджест виходить на першому запуску за добу у вікні
>   6:00–21:00; через діри перший вдалий припав на вечір. Легіт-функції (war_stats + digest) — не баг.
> • РІШЕННЯ (вибір власника): зовнішній тригер cron-job.org → GitHub API `workflow_dispatch` щопівгодини.
>   Воркфлоу вже має `workflow_dispatch` + `concurrency` (накладання безпечне), тож код не міняв.
>   Написано EXTERNAL_TRIGGER.md — PAT (Actions RW), налаштування cron-job.org, перевірка (HTTP 204), безпека токена.
> ВЕРИФІКОВАНО: назва воркфлоу news_bot.yml і наявність workflow_dispatch перевірені в файлі; побудований
>   dispatch-URL/заголовки/боді — за поточним GitHub REST (api.github.com/.../workflows/news_bot.yml/dispatches, {"ref":"main"}).
>   Живий тест dispatch робить власник (потрібен PAT — секрет).
> КРОКИ ВЛАСНИКА:
>   1) Виконати EXTERNAL_TRIGGER.md: створити fine-grained PAT (repo telegram-news-bot, Actions: Read and write),
>      завести завдання на cron-job.org (POST на dispatch-URL, 3 заголовки, body {"ref":"main"}), Test run → чекати HTTP 204.
>   2) Переконатись у GitHub → Actions, що з'явився прогон з тригером workflow_dispatch, і у фідбеку — підсумок.
>   3) Закомітити доки: git add EXTERNAL_TRIGGER.md STATUS.md BUGS.md && git commit -m "Зовнішній тригер: обхід дропання крона GitHub" && git push.
>      (Токен НЕ комітити — він лише в cron-job.org.)
> ⏭ НАСТУПНА СЕСІЯ: (а) `digest.py` — mark_today("morning") при падінні digest губить дайджест на добу; (б) погода
>   «дані недоступні» для ~10 міст (ліміт Open-Meteo?); (в) підтвердити оживлення RSS-фідів (БАГ-009); БАГ-006, БАГ-007.

> ✅ ЦЯ СЕСІЯ (2026-07-13, 5-а — [OPUS]) — «мало новин»: 8/22 фідів «мертві» = блокування User-Agent.
> ЗАЧІПАЄ: bot.py (новий parse_feed), feed_check.py.  НАСЛІДКИ ЗАПУСКУ: нічого (адитивно; secret/cron без змін).
> ЩО ЗРОБЛЕНО:
> • Діагноз: канал живий (фідбек «опубліковано 3 з 12»), проблема — низький обсяг. feedparser.parse(url)
>   шле власний UA `feedparser/6.0.12`, який ріжуть UNIAN/Suspilne/DW/LIGA/Mind/Korrespondent → порожньо → фолс-«dead».
> • bot.py: додано `parse_feed(url)` — тягне через requests з браузерним UA (FEED_UA), байти → feedparser;
>   fallback на прямий feedparser.parse при винятку/HTTP≠200/порожньо. `fetch_news` тепер кличе parse_feed.
> • feed_check.py: імпортує і використовує той самий parse_feed (єдина логіка UA).
> • «Битий Markdown» у звіті RSS — НЕ баг: notify_admin шле без parse_mode (чистий текст); злиплі лінки —
>   артефакт копіювання з Telegram-клієнта. Код не чіпав.
> ВЕРИФІКОВАНО: ast.parse bot.py+feed_check.py OK; parse_feed на моках — 200→1 запис, виняток→fallback,
>   403→fallback (усі PASS). Реальні фіди з пісочниці недоступні (проксі) — оживлення перевіряє власник (крок 3).
> ⚠ ІНЦИДЕНТ: перша спроба Edit обрізала bot.py і feed_check.py посеред кирилиці (CRLF/UTF-8). Відновлено з
>   `git show HEAD:<файл>`, патч застосовано Python-байтами. Обидва файли цілі й валідні (ast OK).
> ⚠ GIT: `.git/index` занулено + висить порожній `.git/index.lock` (не зняти з пісочниці). Лагодиться в кроці 1.
> КРОКИ ВЛАСНИКА:
>   1) Полагодити індекс (у папці проекту, cmd): `del .git\index.lock` → `del .git\index` → `git reset` → `git status`
>      (покаже змінені bot.py, feed_check.py, STATUS.md, BUGS.md; робочі файли не постраждають).
>   2) `git add bot.py feed_check.py STATUS.md BUGS.md`
>      `git commit -m "RSS: браузерний User-Agent (parse_feed) — оживити заблоковані фіди"`
>      `git pull --rebase` (підтягнути 9579c5b — оновлення published.db воркфлоу) → `git push`.
>   3) Запустити воркфлоу (workflow_dispatch), глянути фідбек «🩺 Перевірка RSS»: мертвих має стати помітно менше
>      (з 8 ближче до 2). Якщо suspilne/unian/dw/liga/mind/korrespondent ожили — фікс спрацював.
>   4) Якщо radiosvoboda (/api/zrqiteoiqp) і oporaua (/feed) лишаться мертвими — вони справді протухли; скажи, підберу заміну.
> ⏭ НАСТУПНА СЕСІЯ: за звітом власника — прибрати/замінити реально мертві radiosvoboda/oporaua; БАГ-006 (якість), БАГ-007 (Twitter 401).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 2-а — [OPUS]) — Gemini 404: справжня причина vs оманливий лог.
> ЗАЧІПАЄ: bot.py, gemini_check.py (новий), BUGS.md.  НАСЛІДКИ ЗАПУСКУ: нічого (адитивно, без нових secret/cron).
> ЩО ЗРОБЛЕНО:
> • Розібрано хибну тривогу: URL Gemini у bot.py:78 цілий і правильний. «...generativelanguage.google»
>   у підсумку адміну — це рівно 70 перших символів помилки, бо лог робив `str(e)[:70]`. Домен не зламаний.
> • `call_llm`: додано гілку `status >= 400`, що логує ТІЛО відповіді Google (справжня причина 404) —
>   до 300 симв. у консоль, до 120 у підсумок адміну; обрізання в `except` розширено 70→120.
> • Новий `gemini_check.py` — окрема діагностика (список моделей + тест-виклик), не чіпає бойовий bot.py.
> • БАГ-008 у BUGS.md (двошарова причина: обрізання логу + справжній 404 з боку Google).
> ВЕРИФІКОВАНО: py_compile bot.py і gemini_check.py; логіку нової гілки прогнано на фейкових відповідях
>   (429 → «ліміт», 404 з тілом → причина в логу, 200 → контент повертається). Бойовий ключ не смикав.
> КРОКИ ВЛАСНИКА: 1) `git add bot.py gemini_check.py BUGS.md STATUS.md && git commit && git push`.
>   2) Локально запустити `GEMINI_API_KEY=... python gemini_check.py` → глянути, модель чи API винні.
>   3) За результатом: виправити назву моделі в bot.py:80 АБО ввімкнути Generative Language API. Це роблю я? — скажи.
> ⏭ НАСТУПНА СЕСІЯ: за виводом gemini_check закрити БАГ-008 (модель/ключ); поспостерігати БАГ-006 (якість).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 3-я — [OPUS]) — БАГ-008: причина знайдена, модель замінено.
> ЗАЧІПАЄ: bot.py (рядок 80), gemini_check.py, BUGS.md.  НАСЛІДКИ ЗАПУСКУ: нічого (заміна назви моделі; secret/cron без змін).
> ЩО ЗРОБЛЕНО:
> • Власник прогнав gemini_check.py з бойовим ключем. Діагноз точний: ListModels → HTTP 200,
>   `gemini-2.5-flash` У СПИСКУ Є, але генерація дає 404 «This model ... is no longer available to
>   new users. Please update your code to use a newer model.» Тобто Google закрив модель для нових
>   користувачів — не URL, не ключ, не вимкнений API. Клас БАГ-001.
> • bot.py:80 `gemini-2.5-flash` → `gemini-3.5-flash` (актуальна GA зі списку акаунта).
> • gemini_check.py: п.2 тепер перебирає кандидатів (3.5-flash, 2.5-flash-lite, 2.0-flash,
>   flash-latest) через РЕАЛЬНИЙ виклик генерації і друкує, які 200. Присутність у ListModels
>   не гарантує доступності — цей баг саме це й показав.
> ВЕРИФІКОВАНО: py_compile gemini_check.py OK; bot.py — заміна одного рядка-моделі (решта не чіпалась).
>   Бойовий Gemini-виклик робить власник (крок нижче).
> КРОКИ ВЛАСНИКА: 1) Ще раз `python gemini_check.py` — переконатись, що рядок `gemini-3.5-flash: HTTP 200`
>   і в підсумку «✅ робоча». Якщо ні — у виводі буде підказка, на що замінити (перший зелений кандидат),
>   скажи мені або постав його сам у bot.py:80. 2) `git add bot.py gemini_check.py BUGS.md STATUS.md &&
>   git commit -m "БАГ-008: Gemini модель 2.5-flash→3.5-flash + лог тіла помилки" && git push`.
> ⏭ НАСТУПНА СЕСІЯ: як власник підтвердить 200 на 3.5-flash — закрити БАГ-008 у ✅; далі БАГ-006 (якість),
>   БАГ-007 (Twitter 401).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 4-а — [OPUS]) — БАГ-008 ЗАКРИТО: gemini-3.5-flash підтверджено валідною.
> ЗАЧІПАЄ: gemini_check.py (класифікація статусів), BUGS.md (БАГ-008 → Виправлені).  НАСЛІДКИ ЗАПУСКУ: нічого.
> ЩО ЗРОБЛЕНО:
> • Власник прогнав оновлений gemini_check.py. Результат: `gemini-3.5-flash` → HTTP 503 (НЕ 404!) —
>   тобто назва моделі ВАЛІДНА, резолвиться; 503 = транзієнтне «high demand». `2.5-flash-lite` → 404
>   (теж закрита для нових), `2.0-flash` → 429 (квота), `flash-latest` → 503. Отже вибір 3.5-flash вірний.
> • gemini_check.py: підсумок був занадто грубий (503/429 рахував як провал). Переписано класифікацію:
>   200=працює, 404=закрита(міняти), 503/429=назва валідна(транзієнт/квота). Тепер не вводить в оману.
> • БАГ-008 перенесено у ✅ Виправлені. Ретрай на 503 у bot.py навмисно НЕ додавав — Gemini резервний
>   (останній у ланцюзі), fallback + щогодинний цикл покривають; зайва складність порушила б «мінімум зламів».
> ВЕРИФІКОВАНО: py_compile gemini_check.py OK. Бойові виклики Gemini — власник (діагностика вище).
> КРОКИ ВЛАСНИКА: 1) Закомітити ВСЕ з цієї+минулої під-сесії:
>   `git add bot.py gemini_check.py BUGS.md STATUS.md && git commit -m "БАГ-008: Gemini 2.5-flash→3.5-flash, лог тіла помилки, діаг" && git push`.
>   (Окремий commit-крок з минулого блоку більше не потрібен — цей його заміщує.)
> ⏭ НАСТУПНА СЕСІЯ: БАГ-006 (якість переказу — спостереження на 3.5-flash як резерві), БАГ-007 (Twitter 401).

**Оновлено 2026-07-13 (Z-4 дедуп подій):** власник помітив дубль (та сама подія
з різних джерел). Додано в `bot.py` `is_duplicate_title` — перед публікацією
порівнює заголовок кандидата з опублікованими за 24 год (Жаккар на префіксах слів,
поріг 0.5). Ловить дублі з різних джерел, але НЕ зливає різні події одного міста
(перевірено 6/6: Херсон-АЗС vs Херсон-автівка лишаються окремими). Комітити `bot.py`.
URL-дедуп лишився як був. Схожі заголовки тепер не проходять і не витрачають токени.

**Оновлено 2026-07-13 (БАГ-005 закрито):** `war_stats.py` переписано на стабільний
JSON-API втрат (russianwarship.rip) — тепер постить текст із цифрами й приростом
за добу замість крихкої картинки з сайту ЗСУ (той став суцільним JS). Дедуп раз/добу
збережено. Верифіковано на реальних даних API. Комітити `war_stats.py`, `BUGS.md`.

**Останнє оновлення:** 2026-07-13, 1-а сесія (Cowork, [OPUS]) — старт за схемою:
інструкції проекту, ROADMAP; Z-2 (модель → 70b) + Z-1 (промпт проти помилок) +
Z-3 (спам втрат); Z-2b (ланцюг провайдерів Cerebras→Groq→Gemini). ⚠ Знайдено:
Twitter віддає 401 Unauthorized (див. блок нижче — питання прав/ключів).

---

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, авто-ревізія джерел + курс НБУ — [OPUS]).
> ЗАЧІПАЄ: нове `feed_check.py`, `scheduler.py`, `digest.py`.
> ПЕРЕВІРКА ГІТА: власник усе зробив правильно — коміт 985efb8 видалив
> __pycache__/*.pyc і додав .gitignore; репо чистий. («index corrupt» у пісочниці
> — глюк git через Windows-індекс, репо не стосується.)
>
> ЩО ЗРОБЛЕНО:
> • Авто-ревізія RSS: новий `feed_check.py` — раз на добу парсить усі фіди й шле
>   адміну список мертвих/порожніх (або «усі живі ✅»). Scheduler викликає його
>   раз/добу (daily_log "feed_check", від 6:00). Так ревізія джерел автоматична.
> • Курс НБУ у ранковому дайджесті: `get_rates()` у digest.py (безкоштовний API
>   bank.gov.ua, без ключа) → рядок «💵 Долар … 💶 Євро …». Захищено try/except:
>   якщо API не відповість — дайджест просто без курсу.
>
> ВЕРИФІКОВАНО: feed_check `py_compile` OK; scheduler (хост) — блок feed_check
>   коректний; парсинг курсу на зразку відповіді НБУ — правильний формат.
>   (NBU API через web_fetch віддав порожньо — структура задокументована й стандартна;
>   підтвердимо з реального ранкового дайджесту. Пісочниця й далі ріже великі файли — хост.)
>
> КРОКИ ВЛАСНИКА: закомітити `feed_check.py`, `scheduler.py`, `digest.py`, push.
> Завтра вранці має прийти: дайджест із курсом + звіт feed_check (які RSS мертві).
>
> ⏭ ДАЛІ: БАГ-005 — полагодити `war_stats.py` (розібрати поточну структуру сторінки
> ЗСУ); потім — почистити мертві фіди за звітом feed_check; ідея з Telegram-джерелами.

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, публічний репо + більше України — [OPUS]).
> ЗАЧІПАЄ: `bot.py`, нове `.gitignore`.
>
> КОНТЕКСТ: власник зробив репо ПУБЛІЧНИМ (→ Actions-хвилини необмежені, лишаємо
> `*/15`). Провів АУДИТ БЕЗПЕКИ: у коді й у ВСІЙ історії (947 комітів) — жодних
> секретів, `.env` ніколи не було, пошти/chat_id хардкоду нема, усе через
> os.environ/Secrets. Бази — лише історія новин. РЕПО БЕЗПЕЧНО ПУБЛІЧНИЙ.
>
> ЩО ЗРОБЛЕНО:
> • `.gitignore` додано (__pycache__, .env, venv, IDE). У репо були закомічені
>   __pycache__/*.pyc — власник прибирає: `git rm -r --cached __pycache__` +
>   commit/push.
> • Більше про Україну: додано 2 перевірені воєнні RSS (armyinform.com.ua/feed,
>   militarnyi.com/uk/feed); нова функція `ukraine_score` — новини про Україну
>   сортуються ПЕРШИМИ (лог-тест: воєнне 8/4 бали вище за науку/іноземне 0), але
>   різноманіття лишається. MAX_POSTS_PER_RUN 2→3.
>
> ВЕРИФІКОВАНО: ukraine_score — сорт коректний (Україна першою); нові RSS
> віддають валідний application/rss+xml (перевірено web_fetch). Аудит секретів — чисто.
>
> КРОКИ ВЛАСНИКА: закомітити `bot.py` + `.gitignore`; окремо прибрати сміття:
> `git rm -r --cached __pycache__` → commit → push.
>
> ⏭ ДАЛІ (повна ревізія джерел — почато): решту ~20 наявних RSS перевірити на
> «живість». Найрозумніше — додати у підсумок адміну звіт про мертві фіди (щоб
> аудит був автоматичний, а не ручний). + курс НБУ (Z-7), БАГ-005 (war_stats),
> ідея власника: тягнути з офіційних Telegram-каналів медіа/держустанов (t.me/s).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, «живий режим» + оцінка якості — [OPUS]).
> ЗАЧІПАЄ: `scheduler.py` (переписано), `bot.py`, `.github/workflows/news_bot.yml`.
>
> ВІДКРИТТЯ: кеш `t.me/s` обходиться через `?before=<велике число>` — я бачу
> найсвіжіші пости сам, допомога власника з цим не потрібна.
> ОЦІНКА ЯКОСТІ нових моделей (gemma-4-31b/gpt-oss-120b, пости 3114–3123):
> помітно КРАЩЕ — стисло, без «води», без вигаданих посад/статі; пішла наука/
> здоров'я (фільтр-фікс працює). Дрібниця: калька «кальцифікующего» (підчистимо).
>
> ЩО ЗРОБЛЕНО (Oracle скасовано — картка не варіант; лишаємось на GitHub, безкоштовно):
> • «ЖИВИЙ РЕЖИМ»: cron `0 * * * *` → `*/15 * * * *`; MAX_POSTS_PER_RUN 5→2.
>   Тепер струмочок постів, а не залп раз на годину.
> • `scheduler.py` переписано під часті запуски: новини — щоразу; дайджести й
>   war_stats — РАЗ НА ДОБУ через таблицю `daily_log` (morning від 6:00, evening
>   від 21:00, war_stats від 6:00). Захист від повторів.
> • Воркфлоу: `concurrency` group — без накладання прогонів (щоб не було git-push
>   конфліктів при 96 запусках/добу).
> • notify_admin шле підсумок ЛИШЕ коли є пост або помилка (щоб не спамити 96×/добу).
>
> ВЕРИФІКОВАНО: scheduler.py `py_compile` OK; логіка daily_log — 6/6 на чистій БД
> (morning/evening/war_stats незалежні, повторний mark безпечний). (Копію
> published.db пісочниця дала пошкодженою — mount; тест зроблено на чистій БД.)
>
> КРОКИ ВЛАСНИКА: закомітити `scheduler.py`, `bot.py`, `news_bot.yml`, push.
> Далі канал сам оживе (пости кожні ~15 хв). Я контролюю через t.me/s ?before + підсумки.
> ⏭ ДАЛІ: підчистити дрібні кальки промптом; полагодити war_stats (БАГ-005);
> Z-7 (курс НБУ). Постійний воркер — відкладено (без картки; колись — VPS/домашній пристрій).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, підтвердження здоров'я — [OPUS]).
> ПІДСУМОК ВІД БОТА (перший реальний): «опубліковано 5 з 12; Cerebras×3, Groq×2;
> Gemini 404; Cerebras 429 (один раз, перемкнувся)». ВИСНОВОК: пайплайн ЗДОРОВИЙ,
> самодіагностика працює. Попередній «0 постів» був через відсутність свіжих новин.
> • Gemini 404: ендпоінт/модель правильні (перевірено в доках Google) → причина в
>   ключі/проекті. Переставив Gemini в КІНЕЦЬ черги: Cerebras→Groq→Gemini. Поки два
>   робочі відповідають, Gemini не викликається — 404 зникає. Полагодити ключ згодом.
> • Нюанс видимості: `t.me/s` кешується — найновіші пости я бачу із затримкою;
>   найшвидша правда — підсумок від бота.
> КРОК ВЛАСНИКА: закомітити `bot.py`, push. Оцінку якості нових моделей
> (gemma-4-31b/gpt-oss-120b) зроблю з наступних постів, коли оновиться кеш.
> ⏭ ДАЛІ: старт міграції на постійний воркер (Oracle Free Tier) — власнику
> створити безкоштовну VM; я готую worker.py (черга + APScheduler + локальна БД).

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, самодіагностика — [OPUS]) — прибираємо «сліпоту».
> ЗАЧІПАЄ: `bot.py`, `.github/workflows/news_bot.yml`.
> ПРИЧИНА: після ревізії моделей у веб-версії каналу нових постів не з'явилось —
> або не було свіжих новин, або всі моделі відпали. З обрізаних логів не видно.
>
> ЩО ЗРОБЛЕНО:
> • `notify_admin()` у bot.py: наприкінці кожного запуску шле адміну (через
>   FEEDBACK_BOT_TOKEN/ADMIN_CHAT_ID) короткий підсумок — скільки опубліковано з
>   N кандидатів, які моделі спрацювали (Gemini×2…), які помилки (Cerebras 404…).
> • `call_llm` веде STATS (успіхи/остання помилка на провайдера).
> • Workflow: у крок scheduler додано FEEDBACK_BOT_TOKEN + ADMIN_CHAT_ID.
> ЗАВДЯКИ ЦЬОМУ: замість повних логів власник пересилає 1 рядок — видно стан.
>
> ВЕРИФІКОВАНО: логіка підсумку — 2 сценарії (успіх + повний провал) дають
> правильний текст; блок notify_admin/STATS на хості коректний. (Пісочниця й далі
> б'є mount-артефактом — хост джерело правди.)
>
> КРОКИ ВЛАСНИКА: закомітити `bot.py` + `news_bot.yml`; push; Run workflow →
> у Telegram від фідбек-бота прийде підсумок — перешли мені той рядок.
> (Секрети FEEDBACK_BOT_TOKEN/ADMIN_CHAT_ID уже існують — фідбек-бот ними працює.)
>
> НАПРЯМ (обрано мною за дорученням власника): ідемо до постійного воркера
> (Oracle Free Tier) як фундаменту живого каналу + тривог. Послідовність:
> A) самодіагностика ✅ → B) підтвердити, що пайплайн публікує → C) черга-крапельниця
> (переноситься у воркер) → D) міграція на Oracle Free Tier (постійний процес,
> стан у локальній БД, без git-коммітів). D потребує дії власника (створити VM) —
> підготую покроково.

> ✅ ЦЯ СЕСІЯ (2026-07-13, 1-а, ревізія моделей + якість — [OPUS]) — велике.
> ЗАЧІПАЄ: `bot.py`, `digest.py`; нове `BUGS.md`.
>
> КЛЮЧОВЕ ВІДКРИТТЯ 1: я можу САМ читати канал через `https://t.me/s/novyny_ua_svit`
> (п